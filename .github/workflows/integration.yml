name: Integration — self-hosted (oneAPI + Postgres + Laplace)

# Self-hosted runner with oneAPI, PG 18, .NET 10 access.
# SECURITY: triggers ONLY on push to main + workflow_dispatch.
# DO NOT add `pull_request` triggers — untrusted PR code must NEVER run on the self-hosted runner.
# PR validation is handled by ci.yml on GitHub-hosted disposable VMs.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capabilities:
    name: Verify self-hosted runner capabilities
    runs-on: [self-hosted, laplace]
    steps:
      - uses: actions/checkout@v4

      - name: System info
        run: |
          echo "::group::OS / kernel"
          uname -a
          cat /etc/os-release | grep -E '^(NAME|VERSION)='
          echo "::endgroup::"

          echo "::group::CPU + AVX support"
          lscpu | grep -E 'Model name|Architecture|^CPU\(s\):'
          grep -oE 'avx[a-z0-9_]*' /proc/cpuinfo | sort -u
          echo "::endgroup::"

          echo "::group::Memory"
          free -h | head -3
          echo "::endgroup::"

      - name: Intel oneAPI present
        run: |
          if [ -d /opt/intel/oneapi ]; then
            echo "✓ oneAPI installed at /opt/intel/oneapi"
            ls /opt/intel/oneapi/compiler/latest/bin/ | head -10
          else
            echo "::error::Intel oneAPI not found at /opt/intel/oneapi"
            exit 1
          fi

      - name: PostgreSQL + PostGIS
        run: |
          if command -v pg_config >/dev/null 2>&1; then
            pg_config --version
            pg_config --includedir-server
          else
            echo "::error::pg_config not found"
            exit 1
          fi

      - name: .NET SDK
        run: |
          if command -v dotnet >/dev/null 2>&1; then
            dotnet --version
            dotnet --list-sdks
          else
            echo "::error::dotnet not found"
            exit 1
          fi

      - name: Eigen + ICU + xxHash
        run: |
          pkg-config --modversion eigen3 || echo "::warning::eigen3 pkg-config not found"
          pkg-config --modversion icu-uc || echo "::warning::ICU not found"
          dpkg -l libxxhash0 2>/dev/null | grep -q "^ii" && echo "✓ libxxhash0 installed" || echo "::warning::libxxhash0 not installed"

  build:
    name: Build engine + extension + app
    needs: capabilities
    runs-on: [self-hosted, laplace]
    steps:
      - uses: actions/checkout@v4

      - name: Build engine (CMake + Ninja, fetches Spectra + BLAKE3)
        run: |
          cd engine
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
          cmake --build build

      - name: Run engine tests
        run: |
          cd engine/build
          ctest --output-on-failure

      - name: Build PG extension (PGXS)
        run: |
          cd extension
          make PG_CONFIG=/usr/lib/postgresql/18/bin/pg_config

      - name: Build C# app (dotnet)
        run: |
          cd app
          dotnet build Laplace.slnx -c Release

  extension-smoke-test:
    name: Install extension + create test DB + smoke test
    needs: build
    runs-on: [self-hosted, laplace]
    steps:
      - uses: actions/checkout@v4

      - name: Build PG extension
        run: |
          cd extension
          make PG_CONFIG=/usr/lib/postgresql/18/bin/pg_config

      - name: Install extension into PG18
        # Requires NOPASSWD sudo for /usr/bin/make for the runner user.
        # See one-time setup in CONTRIBUTING.md.
        run: |
          cd extension
          sudo make install PG_CONFIG=/usr/lib/postgresql/18/bin/pg_config

      - name: Create ephemeral test database
        # Requires PG superuser role for the runner user (ahart).
        # See one-time setup in CONTRIBUTING.md.
        run: |
          TESTDB="laplace_ci_${{ github.run_id }}"
          echo "TESTDB=$TESTDB" >> $GITHUB_ENV
          dropdb --if-exists "$TESTDB" || true
          createdb "$TESTDB"
          echo "::notice::Created test DB $TESTDB"

      - name: Install extensions + smoke test
        run: |
          # Install PostGIS (required by RULES.md R1)
          psql -d "$TESTDB" -c "CREATE EXTENSION IF NOT EXISTS postgis;"

          # Install Laplace extension
          psql -d "$TESTDB" -c "CREATE EXTENSION laplace;"

          # Smoke test: laplace_version() returns the expected string
          RESULT=$(psql -tA -d "$TESTDB" -c "SELECT laplace_version();")
          echo "laplace_version() returned: '$RESULT'"
          if [ "$RESULT" != "0.1.0" ]; then
            echo "::error::Expected '0.1.0', got '$RESULT'"
            exit 1
          fi
          echo "::notice::laplace_version() OK"

          # Verify both extensions present
          psql -d "$TESTDB" -c "\dx" -P "footer=off"

      - name: Cleanup test database
        if: always()
        run: |
          if [ -n "$TESTDB" ]; then
            dropdb --if-exists "$TESTDB" || true
            echo "::notice::Cleaned up $TESTDB"
          fi

  # Future jobs (per .agent/status/plan.md):
  # - pg_regress test-extension (Chunk 2+; after laplace--*.sql has real functions)
  # - test-app (dotnet test) (Chunk 1+; after C# tests exist)
  # - verify-determinism, verify-fk, verify-perfcache (Chunk 3+)
  # - round-trip end-to-end test (Chunk 8)
  # All trigger only on push to main / workflow_dispatch — never PRs from forks.
