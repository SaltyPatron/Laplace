name: Measure — benchmark

# Explicit calibration/evidence lane. The complete measured preparation and run
# hold the real host file reservation; there is no Actions-level concurrency group
# that can silently replace a pending operator request.
on:
  workflow_dispatch:
    inputs:
      target_ref:
        description: "repository ref/SHA to benchmark (blank = dispatched revision)"
        type: string
        required: false
        default: ""
      suite:
        description: "versioned benchmark suite"
        type: choice
        required: true
        default: quick
        options: [quick, throughput, core, scale, dag, moby, query, chess, geometry, recorded, all]
      repeats:
        description: "repeats per core/scaling point; all runs retained, best reported"
        type: string
        required: true
        default: "3"
      scale_workers:
        description: "optional comma-separated scale points; blank derives serviceable host points"
        type: string
        required: false
        default: ""
      reserve_logical_cpus:
        description: "logical CPUs reserved for PostgreSQL/runner/services during scale suites"
        type: string
        required: true
        default: "2"
      allow_saturation:
        description: "explicitly allow a full-logical-CPU scaling point"
        type: boolean
        default: false
      corpus_dir:
        description: "core benchmark corpus root (blank = repository)"
        type: string
        required: false
        default: ""
      moby_path:
        description: "Moby Dick fixture path on the measured host"
        type: string
        required: true
        default: "/vault/Data/test-data/text/moby_dick.txt"
      force_rebuild:
        description: "force a clean exact-revision rebuild before measurement"
        type: boolean
        default: false

permissions:
  contents: read

env:
  LAPLACE_EXTERNAL: ${{ vars.LAPLACE_EXTERNAL || '/build/external' }}
  LAPLACE_INSTALL_PREFIX: ${{ vars.LAPLACE_INSTALL_PREFIX || '/opt/laplace' }}
  LAPLACE_PG_PREFIX: ${{ vars.LAPLACE_PG_PREFIX || '/opt/laplace/pgsql-18' }}
  PGDATABASE: laplace
  LAPLACE_DATA_ROOT: /vault/Data

jobs:
  benchmark:
    name: Measured host — versioned benchmark suite
    runs-on: [self-hosted, laplace]
    timeout-minutes: 180
    steps:
      - name: Create immutable evidence directory first
        shell: bash
        run: |
          set -euo pipefail
          receipt="$RUNNER_TEMP/laplace-benchmark-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          rm -rf "$receipt"
          mkdir -p "$receipt"
          printf 'LAPLACE_BENCH_RECEIPT=%s\n' "$receipt" >> "$GITHUB_ENV"

      - name: Select exact target and run immutable benchmark driver under one host reservation
        shell: bash
        env:
          DISPATCH_SHA: ${{ github.sha }}
          REQUESTED_REF: ${{ inputs.target_ref }}
          CHECKOUT_TOKEN: ${{ github.token }}
          SUITE: ${{ inputs.suite }}
          REPEATS: ${{ inputs.repeats }}
          SCALE_WORKERS: ${{ inputs.scale_workers }}
          RESERVE_LOGICAL_CPUS: ${{ inputs.reserve_logical_cpus }}
          ALLOW_SATURATION: ${{ inputs.allow_saturation }}
          CORPUS_DIR: ${{ inputs.corpus_dir }}
          MOBY_PATH: ${{ inputs.moby_path }}
          FORCE_REBUILD: ${{ inputs.force_rebuild }}
        run: |
          set -euo pipefail
          umask 0002

          lock_root="${LAPLACE_WORK_ROOT:-/build/laplace/work}"
          mkdir -p "$lock_root"
          exec 9>"$lock_root/host-resource.lock"
          flock 9

          target="${REQUESTED_REF:-}"
          [[ -n "$target" ]] || target="$DISPATCH_SHA"
          printf 'requested_ref=%s\nworkflow_sha=%s\n' "$target" "$DISPATCH_SHA" > "$LAPLACE_BENCH_RECEIPT/request.env"

          cd "$GITHUB_WORKSPACE"
          if [[ ! -e .git ]]; then
            [[ -z "$(find . -mindepth 1 -maxdepth 1 -print -quit)" ]] || {
              echo "::error::nonempty benchmark workspace has no repository; preserving its files" >&2
              exit 1
            }
            git init --initial-branch=main
            git remote add origin "https://github.com/$GITHUB_REPOSITORY.git"
          fi
          git diff --quiet
          git diff --cached --quiet

          checkout_auth="$(printf 'x-access-token:%s' "$CHECKOUT_TOKEN" | base64 -w0)"
          driver="$RUNNER_TEMP/benchmark-evidence-ci-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}.sh"

          GIT_CONFIG_COUNT=1 \
            GIT_CONFIG_KEY_0=http.https://github.com/.extraheader \
            GIT_CONFIG_VALUE_0="AUTHORIZATION: basic $checkout_auth" \
            git fetch --no-tags --depth=1 origin "$DISPATCH_SHA"
          workflow_sha="$(git rev-parse FETCH_HEAD)"
          [[ "$workflow_sha" == "$DISPATCH_SHA" ]]
          git show "$workflow_sha:scripts/benchmark-evidence-ci.sh" > "$driver"
          chmod 700 "$driver"
          printf 'driver_sha256=%s\n' "$(sha256sum "$driver" | awk '{print $1}')" >> "$LAPLACE_BENCH_RECEIPT/request.env"

          GIT_CONFIG_COUNT=1 \
            GIT_CONFIG_KEY_0=http.https://github.com/.extraheader \
            GIT_CONFIG_VALUE_0="AUTHORIZATION: basic $checkout_auth" \
            git fetch --no-tags --prune origin "$target"
          unset checkout_auth CHECKOUT_TOKEN

          resolved="$(git rev-parse FETCH_HEAD)"
          git checkout --no-overwrite-ignore --detach "$resolved"
          [[ "$(git rev-parse HEAD)" == "$resolved" ]]
          git diff --quiet
          git diff --cached --quiet

          export LAPLACE_BENCH_SHA="$resolved"
          printf 'LAPLACE_BENCH_SHA=%s\n' "$resolved" >> "$GITHUB_ENV"
          printf 'resolved_sha=%s\n' "$resolved" >> "$LAPLACE_BENCH_RECEIPT/request.env"

          exec bash "$driver"

      - name: Publish benchmark summary
        if: always()
        shell: bash
        env:
          SUITE: ${{ inputs.suite }}
        run: |
          sha="${LAPLACE_BENCH_SHA:-unresolved}"
          {
            echo '## Laplace versioned benchmark suite'
            echo
            echo "- Commit: \`$sha\`"
            echo "- Runner: \`$RUNNER_NAME\`"
            echo "- Suite: \`$SUITE\`"
            echo "- Artifact: \`laplace-benchmark-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT\`"
            if [[ -f "$LAPLACE_BENCH_RECEIPT/suite-receipt.json" ]]; then
              echo
              python3 - "$LAPLACE_BENCH_RECEIPT/suite-receipt.json" <<'PY'
          import json, sys
          receipt = json.load(open(sys.argv[1], encoding='utf-8'))
          for item in receipt['results']:
              profile = item['profile']
              result = item['result']
              print(f"### {profile}")
              print()
              if profile == 'core-single':
                  print(f"- codepoints/s: {result['codepoints_per_second']:,.0f}")
                  print(f"- 4-char BPE-equivalent/s: {result['bpe_equivalent_tokens_per_second_4chars']:,.0f}")
                  print(f"- tier-tree nodes/s: {result['tier_tree_nodes_per_second']:,.0f}")
              elif profile == 'core-scale':
                  best = max(result['points'], key=lambda p: p['bpe_equivalent_tokens_per_second_4chars'])
                  print(f"- best measured workers: {best['workers']}")
                  print(f"- best 4-char BPE-equivalent/s: {best['bpe_equivalent_tokens_per_second_4chars']:,.0f}")
                  print(f"- speedup vs 1 worker: {best['speedup_vs_1_worker']:.3f}x")
                  print(f"- physical cores / allowed logical CPUs: {result['physical_cores']} / {result['allowed_logical_cpus']}")
              elif profile == 'core-dag-scale':
                  best = max(result['points'], key=lambda p: p['speedup_vs_1_worker'])
                  semantic = result['semantic_oracle']
                  print(f"- fixed semantic objects/repeat: {result['semantic_objects_per_repeat']}")
                  print(f"- input sha256: {result['input_sha256']}")
                  print(f"- root id: {semantic['root_id']}")
                  print(f"- complete tree sha256: {semantic['tree_fingerprint_sha256']}")
                  print(f"- best measured workers: {best['workers']}")
                  print(f"- speedup vs 1 worker: {best['speedup_vs_1_worker']:.3f}x")
                  print(f"- parallel efficiency: {best['parallel_efficiency']:.3f}")
                  print(f"- physical cores / allowed logical CPUs: {result['physical_cores']} / {result['allowed_logical_cpus']}")
              elif profile == 'moby-roundtrip':
                  print(f"- codepoints: {result['codepoints']:,}")
                  print(f"- ingest: {result['ingest_milliseconds']:.1f} ms")
                  print(f"- export: {result['export_milliseconds']:.1f} ms")
                  print(f"- bit-perfect: {result['bit_perfect']}")
              if item.get('rapl_energy'):
                  energy = sum(x['energy_uj'] for x in item['rapl_energy']) / 1e6
                  print(f"- summed RAPL-domain energy: {energy:.3f} J (not wall energy; domains may overlap)")
              print()
          PY
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Seal evidence manifest
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          out="$LAPLACE_BENCH_RECEIPT"
          find "$out" -type f -printf '%P\n' | sort > "$out/manifest.txt"
          while IFS= read -r relative; do
            [[ "$relative" != artifact-sha256.txt ]] || continue
            sha256sum "$out/$relative"
          done < "$out/manifest.txt" > "$out/artifact-sha256.txt"

      - name: Upload complete benchmark evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: laplace-benchmark-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/laplace-benchmark-${{ github.run_id }}-${{ github.run_attempt }}/
          if-no-files-found: error
          retention-days: 90
