---
name: laplace-invention
description: Complete operating knowledge for every Laplace request and task. Apply automatically before answering or acting so the inventor never has to reteach Laplace.
metadata:
  short-description: Laplace invention operating knowledge
---

# Laplace

Laplace is one deterministic, content-addressed, recursively compositional knowledge
and cognition substrate. It replaces opaque repeated dense-model authority with exact
reusable structure, attributable evidence, uncertainty-bearing standing, sparse indexed
response, coarse native execution, and inspectable receipts. Apply this model
immediately; do not perform ceremonial re-onboarding.

## Compound capabilities that must not be rediscovered

The primitive laws compose into binding product consequences documented in `docs/CAPABILITIES.md`. Do not re-derive them from scratch or narrow them away when working locally.

- Code is grammar-derived recursive structure. Prefer canonical reuse/composition/minimal AST mutation; realize source after structure is selected.
- Exact duplicate AST structure converges by identity. Normalized AST, control/data flow, algebraic form, behavioral evidence and trajectories can expose deeper duplicate implementations.
- Compiler/test/runtime failures and successful fixes are witnessed development trajectories. They should affect later repair and can be compared against other authorized repositories.
- A repository root is the complete application state. Small changes remint only changed ancestry; unchanged subtrees are shared. Checkout/patch artifacts are realizations of root transitions.
- Source/bytecode/executable/container decomposition may continue to control/data/dependency structure and target-machine cycle derivation under explicit ISA/microarchitecture/state assumptions.
- Knowledge scope/authority and compute scope are independent. Knowledge packages/grants govern permitted world/capabilities; hops/fanout/providers/resources govern how deeply/broadly the same intelligence may work.
- Governance is separate from knowledge: retain facts and provenance; explicitly allow/deny/abstain at operation boundaries with WHY/WHY_NOT receipts.
- Tests, gates and benchmark receipts validate the product. Never substitute them for implementing the requested capability.

## Representation

- A finite/countable typed basis forms a countably unbounded family of finite recursive
  compositions. Current widths are machine windows, never invention limits.
- Same canonical content under the same recipe produces the same BLAKE3-derived
  Hash128. Normal convergence is deduplication, not a cryptographic collision.
- Multi-child identity is Merkle-style over ordered child ids. Tier is excluded from
  current content identity. Declared single-child composition collapses to the child.
- Tier is compositional altitude/floor in a modality grammar, not identity or ontology.
  Reobservation reuses structure while adding attributable occurrence/evidence state.
- Tier-0 atoms lie deterministically on S3. Canonical native parents use the Euclidean
  centroid. Children in the closed unit 4-ball imply their parent and realized path
  segments remain in that ball. Radix, dimension, binary64, Hash128, Unicode, and
  storage capacity do not define the theorem.
- Managed Karcher-mean paths are a real recipe divergence from native centroid paths;
  boundedness does not make their coordinates or Hilbert addresses identical.
- Coordinate equality is deliberately many-to-one with respect to knowledge identity. Ordered
  compositions such as `c,a,t` and `a,c,t` may share a centroid/Karcher location while
  retaining different canonical ids and trajectories. Geometry is one response plane, not a
  uniqueness allocator; dense shared regions are usable state, not an identity failure.

## Universal syntax and AST

- AST is universal Laplace structure, not a programming-language feature. Qualified syntax providers — Tree-sitter grammars, Laplace-native grammars, standards parsers, codecs and decoders — recover exact concrete nodes/records, fields, spans, order, errors and ambiguity.
- A versioned recipe lowers provider-specific concrete syntax into one universal typed AST. Documents, utterances, lexical resources, tables, code/binaries, games, media, model/container formats and future digital modalities use the same structural contract.
- Provider output is not truth and is not canonical content by default. The recipe explicitly dispositions each field/role into canonical content/physicality, occurrence, typed reference, provenance, attributed testimony, deterministic calculation, packaging/reconstruction or unresolved state.
- The same recovered value may participate in multiple classes: e.g. a definition sentence is canonical content with physicality while `sense HAS_DEFINITION sentence` is separately attributed testimony.
- Prefer recipe + universal-AST lowering over source-named emitters and private semantic object models. Tree-sitter is here to supply reusable concrete syntax across a large grammar estate; non-Tree-sitter providers must still lower through the same AST/substrate law.

## Physicality, trajectory, and O(tiers)

- Identity says what structure exists; typed physicality says how it is realized. A
  physicality can hold a real 4D coordinate, 128-bit Hilbert locality address, and exact
  trajectory. Hilbert locality nominates neighbors; it is not identity or meaning.
- Each GeometryZM vertex is a reversible 212-bit carrier: four binary64 components x
  53 bits = 128-bit child id + 16-bit packed ordinal + 16-bit run + 52-bit flags.
- Packed vertices serialize child identity/order; they are not child coordinates.
  Realized curves unpack ids, resolve live child physicalities, and order their real
  coordinates by logical ordinal. Never apply spatial metrics to packed doubles.
- Packed ordinal/run widths are local fields, not composition limits. Logical position
  and split RLE support wider sequences; current tests cover 70,000 constituents.
- Admission builds one native tier tree, deduplicates equal working-set content, probes
  existence in set-sized batches per tier, and emits missing structure plus every
  occurrence/physicality. Database crossings are O(tiers), not O(nodes/atoms/records).
- Source estates enumerate every artifact and disposition. Generic workers stream each
  artifact once through read -> parse -> compose; shared apply persists in bulk.
  Adapters own grammar, never private identity, scheduling, persistence, or consensus.

## One web, typed evidence

Canonical identities join recursive DAGs, trajectories, occurrences, containment,
relations/roles, attestations, contradiction, consensus, provenance/dependence,
source/context/world/time, geometry/Hilbert neighborhoods, calculations, sessions,
bindings, obligations, and execution history.

The primitive is: tug the exact admitted strand; enumerate what responds, by which
indexed route, with what typed force. Converging compatible routes are signal;
dependence roots prevent duplicate corroboration.

Ordinary prompts/documents/media/games/code are exact observations and create zero
semantic attestations on admission. Seeded sources may emit governed testimony.
Deterministic tools/parsers/tablebases/geometry/model runs emit versioned calculations.
Observation, testimony, calculation, and consensus remain distinct. Glicko-2 preserves
rating, RD, volatility, witness breadth, and signed confirm/draw/refute state.
Generated descendants cannot certify themselves.

## Cognition

The whole admitted observation is the root/trunk, with exact occurrences, order,
discourse bindings, scope, and obligations:

```text
RESOLVE -> COUPLE -> ORIENT -> ROUTE -> SCAN -> COMPOSE
        -> PROPOSE -> STEER -> SELECT -> REALIZE -> WITNESS
```

COUPLE lets the whole observation tug every eligible indexed plane before a default
sense/provider/relation policy is frozen. Structure/role, relation, ordinal/gap,
evidence/refutation, rating/RD/volatility, dependence, source/context/time, geometry,
calculation, and provenance remain typed. ORIENT jointly resolves or preserves
ambiguity; ROUTE then selects providers/operators and explicit hops/fanout/frontier/
resource envelopes. Explicit caller constraints are lawful; default masks cannot
manufacture interpretation. Every emitted act updates state and recomputes affected
coupling/frontier. A*, Dijkstra, walks, containment, continuation, geometry, chess
search, and model execution are operators inside the program, never cognition alone.

Functional correspondence: Q = exact observation/bindings/obligations; K = indexed
typed responders; QK = coupling; V = responding structure/evidence/calculations; O =
receipted state update. Typed planes supply named head-like channels, trajectory
supplies position, active state supplies residual continuity, repeated coupling/folding
supplies layers, and substrate/session/perfcache supplies memory. Conventional models
are attributable witnesses/providers and export targets, never hidden authority.

## Execution, product, proof, and action

- PostgreSQL owns durable indexed state/MVCC/transactions/set access. Prepared
  set-sized SPI bridges to native C/C++, which owns repeated algorithms. C#/SQL own
  orchestration/contracts/transport. Apply this grain to every pipeline stage.
- Per-row SPI, per-element PInvoke/SQL, hot recursive CTEs, uncontrolled LATERAL,
  per-call temp state, per-item commits, and scalar-loop batches are defects.
- Sparse selection reduces work; coarse execution removes boundary overhead. SIMD/GPU
  is extra headroom. One knowledge world serves all tiers; tiers vary compute envelopes.
- Product surfaces share browse/rank/entity/evidence/trajectory/neighborhood semantics.
  Domains specialize grammar/presentation, never identity or cognition.
- Match proof to claim: theorem; conformance/property tests; exhaustive finite checks;
  live readback; exact benchmark receipts; and main+CI+install/deploy+operator behavior.
- Any discovered gap maps directly to implementation, verification, integration,
  deployment/readback, and stale-tracking correction. It never maps to a generic
  warning, disclaimer, progress performance, request for reteaching, or substitute
  report.
- Never use global negative refrains such as `Laplace is unfinished`, `Laplace is not
  delivered`, `this is not the full invention`, or `not production-ready` as a
  recurring response. Name the exact mechanism and evidence boundary, then continue
  the repair. Global completion language is appropriate only when the inventor
  explicitly asks that exact status question.
- Never turn a correction into homework for a later agent. Repair the authoritative
  invention/architecture record and the existing owning issue in the same workstream;
  if code/runtime work is in scope, continue into that work rather than stopping at
  prose, a checklist, a test-only gate, or a new ticket.
- Never replace accepted behavior with an MVP, demo, scaffold, fallback, private
  engine, reduced model, or review-only artifact. Never use unrelated external
  material or arguments about blame/motive/legal framing to redirect a technical
  Laplace task away from repository evidence and corrective work.

Stable authority: `docs/INVENTION.md`, `docs/INVENTIONS.md`, binding specs
05/06/08/09/11/33/34/36/37. Stable implementation anchors: native hash/composer/math/
Super-Fibonacci/Hilbert/mantissa/trajectory; `ContentTierSpine.cs`;
`ContentBatch.cs`; `cognition_program.c`; `prompt_intent.h`;
`query_evidence.c`; `prompt_structure.c`; `trajectory_continuations.c`.
Inspect them only for task-specific current details.
