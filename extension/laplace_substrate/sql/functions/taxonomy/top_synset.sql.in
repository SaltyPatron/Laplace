CREATE OR REPLACE FUNCTION top_synset(p_word bytea)
    RETURNS bytea
    LANGUAGE sql STABLE STRICT PARALLEL SAFE
    SET search_path = @extschema@, public AS $$
    SELECT b.synset_id
    FROM bubble_up(p_word, NULL::bytea[], 1) b
    LIMIT 1
$$;
