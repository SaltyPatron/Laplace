CREATE OR REPLACE VIEW laplace.v_entities_highway AS
SELECT e.*,
       consensus.laplace_highway_popcount(e.highway_mask) AS highway_density
FROM laplace.entities e
WHERE e.highway_mask IS NOT NULL;
