#!/bin/bash
# scripts/verify-perfcache.sh
# Cross-verify the perf-cache binary against the DB-seeded T0 entities.
# Both artifacts should be byte-identical when derived from the same UCD version.
# Real implementation lands in Chunk 3.

echo "Perf-cache vs DB verification not yet implemented (lands in Chunk 3)" >&2
exit 1
