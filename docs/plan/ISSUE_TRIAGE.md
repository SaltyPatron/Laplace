# Open-issue triage — 2026-09-25

Every open issue (319) was classified against the invention (`docs/INVENTION.md`, `docs/INVENTIONS.md`, binding specs) and the inventor's 2026-09-25 corrections (`docs/plan/ASSIMILATION_ROADMAP.md`, epic #1725). Eight read-only agents did the first pass and I merged it. I overrode verdicts where the documents conflict with each other (§1).

**Do not implement an issue listed as superseded or stale.** An absorbed issue's work happens in its owner, and the kernel worth carrying is noted here and on the owner. Decisions (§1) block the issues they name.

| Verdict | Count |
|---|---|
| Keep | 133 (P0 3, P1 19, P2 48, P3 63) |
| Absorbed into an owner | 87 |
| Superseded by current law | 41 |
| Stale (already done or no longer exists) | 55 |
| Inventor decision | 3 (plus the flagged ones in §1) |

Priority: **P0** blocks the next slice (one coherent reseed, then live forward-program reads). **P1** is the core machine. **P2** is capability on the core. **P3** is later or peripheral. Export (Mold-a-Model) is P3 until the forward program reads correctly: every export slot is generated by querying standing (inventor).

## 1. Decisions for the inventor

| Issue | Title | Question |
|---|---|---|
| #451 | substrate: witness-trajectory evidence virtualization (O(witnesses) rows -> O(facts) rows + testimony vertices) | NEEDS INVENTOR DECISION: testimony vertices on the fact instead of attestation rows; interacts with per-witness replacement. |
| #467 | engine: decide the fate of karcher_mean / log_s3 / exp_s3 — test-only callers today | NEEDS INVENTOR DECISION: karcher_mean now has a non-test caller; log/exp_s3 test-only. |
| #472 | model-lane: rank-aware OV factor storage (v_h + per-head O-basis) before the TinyLlama gate | The documents conflict: model design §4 allows deriving pairs from retained factors, while §1 and #1344 treat factor values as transient operands or forbidden payload. Can derived low-rank circuit factors be durable? |
| #504 | ingest: decide the git-lane relation ledger (reuse vs new) BEFORE codegen — one reseed should pay for it | NEEDS INVENTOR DECISION: git-lane relations reserved; respect one relation per meaning and highway budget. |
| #515 | foundry: tier-scheduled layer operators — replace all-ops-every-layer with the resolution ladder | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). NEEDS INVENTOR DECISION: derived layer/operator schedule (spec 09/12); rebase off archived specs. |
| #535 | substrate-law: write the doc 08 amendment — derivable-evidence virtualization law | Same factor question as #472: can pair evidence be derived at query time from retained factor physicalities (design §4), or must graded evidence be materialized? |
| #548 | ingest: XPOS tags are minted unnamespaced (bare NodeHash) while UPOS goes through the governed resolver | XPOS anchors now OfCanonical string ids; NEEDS INVENTOR DECISION on XPOS (governed tagset registry vs UPOS+features). |
| #799 | EPIC: text content, file artifact, document structure, and work identity are distinct reusable compositions | NEEDS INVENTOR DECISION: file/artifact occurrence entity for USER documents vs 'files are not content' (stated for curated sources). |
| #840 | chess: Explore ranks Na3 above e4 — the fold is correct, the move cell is unconditioned on who played it | NEEDS INVENTOR DECISION: MOVE cell conditioned on ΔElo resembles withdrawn context dimension. |
| #1044 | tier-0 admission: all 1,114,240 codepoints are placed on the glome — including 2,048 surrogates that cannot appear in any UTF-8 content and ~753k codepoints with no recorded properties | NEEDS INVENTOR DECISION: surrogates/noncharacters/unassigned get rows/placements? affects the P0 Unicode reseed cost. |
| #1050 | one mean, declared: chess composes with math4d_karcher_mean, text with math4d_centroid — the 'open placement defect' is answered in code but only in one lane | NEEDS INVENTOR DECISION: centroid vs Karcher fork still present (hash_composer vs chess/agent C#). |
| #1099 | Typed relations are stranded in HAS_DEFINITION gloss text — 'capital of France' is not an election defect | capital(France) fact only in gloss text; NEEDS INVENTOR DECISION on mechanism (calculated gloss analysis vs a stating source). |
| #1303 | consensus: source trust is asserted by 79 literals and can never be earned — and it is worth fewer than ten witnesses anyway | The inventor defined trust as the witness's class (WordNet above Wiktionary above subtitles), but INVENTIONS #104 speaks of participants earning their own standing. Can a source's trust also be earned from its track record? |

For #1099 and #756 (capital(France)), a proposed answer that needs no new relation: OEWN states Paris *is an instance of* "national capital" and *is part of* France. A traversal through those two witnessed claims answers capital(France).

## 2. P0: the next slice

| Issue | Title | Why |
|---|---|---|
| #1715 | Standing math: trust sets certainty, relation rank stays out of truth standing | Workstream A. |
| #1720 | Native forward pass over curated knowledge: bindings, key facts, coupling and realization in native code | Workstream F; reword 'standing distribution' to typed standing. |
| #1725 | EPIC: Assimilation roadmap — corpus chat and model ingestion meet in one consensus | Roadmap epic. |

## 3. P1: the core machine

| Issue | Title | Why |
|---|---|---|
| #401 | read-side: generic native KNN driver (candidate generation + ranking in one native call) | structural_neighbors.sql.in still plpgsql; KNN geometry must be a native operator inside the program. |
| #458 | read-side: relate_path + laplace_ancestry still WITH RECURSIVE — lift to the native graph engine | laplace_ancestry still WITH RECURSIVE; single owner of the native path/closure lift (#460, #461). |
| #469 | substrate-law: two independent 256-bit mask algebras — Mask256.cs vs highway_table.c | Mask algebra implemented twice (Mask256.cs and highway_table.c); one native/codegen source. |
| #525 | substrate: highway perfcache blob has no BLAKE3 CRC and no input-fingerprint staleness key (violates spec 33 law 3) | highway_table.c loads without body checksum or input fingerprint (spec 33 law); must fail closed. |
| #755 | Product acceptance: conversation, structural code repair, model consensus, protocol parity and export | Whole-machine acceptance: conversation, parity, code, model consensus with negative control, semantic export. |
| #921 | Conversation: one stateful canonical forward pass across MCP and OpenAI | OpenAI/MCP serve the same program with stateful multi-turn; restate in spec 36 stages. |
| #951 | EPIC: operation/ISA consolidation — one canonical forward program and one semantic owner per operation family | One semantic owner per operation family; firmware builds on it. |
| #1044 | tier-0 admission: all 1,114,240 codepoints are placed on the glome — including 2,048 surrogates that cannot appear in any UTF-8 content and ~753k codepoints with no recorded properties | NEEDS INVENTOR DECISION: surrogates/noncharacters/unassigned get rows/placements? affects the P0 Unicode reseed cost. |
| #1050 | one mean, declared: chess composes with math4d_karcher_mean, text with math4d_centroid — the 'open placement defect' is answered in code but only in one lane | NEEDS INVENTOR DECISION: centroid vs Karcher fork still present (hash_composer vs chess/agent C#). |
| #1443 | P0 ingest correctness: physical read/batch/worker boundaries must not change canonical state | Physical-plan invariance harness as the conformance test for every #1718 provider; reconstruction only for user content. |
| #1712 | One-implementation law: consolidate the realize/label ladder and word_case_variants into one semantic owner with parity gates | word_case_variants per row; consolidate into REALIZE, not over HAS_NAME_ALIAS/canonical_names. |
| #1717 | ETL ownership and order: native reduces and folds in memory; PostgreSQL stores; C# and SQL orchestrate | Workstream C. |
| #1718 | Format decomposers + recipes: one decomposer per standard format, recipes inject it and add only semantics | Workstream D. |
| #1719 | Model ingestion: TinyLlama's circuits as source-scoped Laplace evidence, read through the forward program | Workstream E. |
| #1721 | Remove the remaining fake records and bookkeeping entities | Workstream G. |
| #1722 | Move every curated source onto a recipe generation and retire the legacy decomposers | Workstream H; absorbs #1471/#1472/#1713. |
| #1723 | Vocabulary governance: retire variant relations, entity OR-masks, highway bit budget | Workstream I. |
| #1724 | Process: materialize locally and replay SQL before every push; verify the substrate, not claim counts | Workstream J. |
| #1726 | Personality firmware: the Gödel engine's programs over the operation ISA, detached from the knowledge | Workstream K. |

## 4. P2: capability on the core

**API host**

| Issue | Title | Why |
|---|---|---|
| #933 | API host: enforce one PostgreSQL connection budget and remove per-job chess pools | Unbudgeted data source creation across services. |

**C**

| Issue | Title | Why |
|---|---|---|
| #451 | substrate: witness-trajectory evidence virtualization (O(witnesses) rows -> O(facts) rows + testimony vertices) | NEEDS INVENTOR DECISION: testimony vertices on the fact instead of attestation rows; interacts with per-witness replacement. |

**D**

| Issue | Title | Why |
|---|---|---|
| #452 | Repository as Merkle DAG/application root: history, structural reuse, and root-transition patching | Git repository ingest as a format provider + recipe; OIDs are external references, never salts. |
| #1049 | artifact/file composition, provenance trunks, and open-state refs — separate content identity from occurrence metadata | Artifact/occurrence separation valid for USER content only; not for curated sources. |

**D (media)**

| Issue | Title | Why |
|---|---|---|
| #1134 | P0 media identity: reusable scalar trajectories plus exact image/audio occurrence and reconstruction state | Media identity/reconstruction consistent with law; not in next slice. |

**D/documents**

| Issue | Title | Why |
|---|---|---|
| #799 | EPIC: text content, file artifact, document structure, and work identity are distinct reusable compositions | NEEDS INVENTOR DECISION: file/artifact occurrence entity for USER documents vs 'files are not content' (stated for curated sources). |

**E**

| Issue | Title | Why |
|---|---|---|
| #486 | foundry: materialize_norm is one clamped scalar broadcast — least substrate-derived materializer | materialize_norm clamped constant in arch_template.cpp:298; export slots must come from standing. |
| #526 | model-lane: build the factor perfcache blob (spec 33 table row / spec 19 candidate b) | Factor ROM designed not installed; wait for #1719 E5 to settle durable factor records. |
| #1056 | generation.foundry_vocab_crawl hangs >400s on the current seed — the scaffold CTE scans 400k trajectories regardless of p_max, and the C# caller sets CommandTimeout=0 | foundry_vocab_crawl still cuts at 400k trajectories regardless of p_max. |

**F**

| Issue | Title | Why |
|---|---|---|
| #1534 | Browse query operands must include witnessed lexical/case variants without collapsing identity | Browse case variants via HAS_CASE_MAPPING + qualifiers, not the retiring variant relations. |
| #1537 | Audit user-facing search surfaces: semantic discovery must use Laplace query/read semantics | Explore misses still redirect to notfound; no hit classes. |

**J**

| Issue | Title | Why |
|---|---|---|
| #1562 | Proof gate: recursively falsify bounded physicality, trajectory closure, and exact reconstruction on the live substrate | Live recursive proof gate never ran; reconstruction only for user content. |

**K**

| Issue | Title | Why |
|---|---|---|
| #436 | governance: adversarial analysis of prompt-injection-as-attestation before the write path opens beyond the operator | Knowledge-authority/governance analysis; meets K's self-witnessing reconciliation. |
| #1420 | product/firmware: COMBINE two arbitrary entities through typed coupling/search and realize a receipted composition | COMBINE as firmware over forward program. |

**OpenAI/MCP surface**

| Issue | Title | Why |
|---|---|---|
| #922 | OpenAI contract: honest roles/tools plus structural code-construction binding | Honest roles/fields/stops/tools. |

**chess**

| Issue | Title | Why |
|---|---|---|
| #447 | chess consensus modeling: self-play trust + move-quality-as-outcome + opening-book drawish poisoning (C07/C08/C09) | C08 conflates calculated move quality with outcomes (spec 08); C07 hardcoded self-play trust vs trust-class law. |
| #449 | chess: ply-cap-adjudicated self-play games recorded as draw on every edge (C14) | ModalityEngine.cs:225 'terminal ?? Draw' feeds false draws into the fold. |
| #491 | IDENTITY (chess): PositionContent/BLAKE3 is canonical; Zobrist is TT/search acceleration only | Transposition convergence proof owed; code side landed (Zobrist only TT key). |
| #574 | chess: grandmaster books — exact document admission plus grounded testimony in the common coupled world | Books as ordinary content through providers + recipes, not a per-source decomposer. |
| #576 | chess: read surface phase 2 — motif queries, book-vs-practice contrast, position similarity | Motif query missing. |
| #605 | chess: Syzygy closings catalog — compose + live proof (probing-oracle framing superseded) | Syzygy WDL/DTZ; 6-men decision open. |
| #818 | EPIC: chess catalog dual + substrate ISA compose — modality stress of the invention | Chess modality stress epic; remove blake3 ids/markers under #1721. |
| #820 | chess: compose shape/DTB/missed-finish over existing ISA surfaces — delete sibling Frechet engines | Chess sibling SQL dialects remain. |
| #821 | chess: live proof — re-seed openings as LINEs, Syzygy closings smoke, QGD entity_curve Frechet | Openings/Syzygy/QGD live proof owed. |
| #835 | chess: ChessPgn corpus is Live-Chess volume + name-splinter identity (partial cancelled seed) | PlayerId is OfCanonical('chess/player/{alias}'); fix via canonical content bindings, not HAS_NAME_ALIAS. |
| #838 | EPIC: chess write-volume — 92% of MOVE cells are single-witness; the reseed batch that fixes it | Batch items; item 6 done; PLAYS_MOVE bit budget. |
| #840 | chess: Explore ranks Na3 above e4 — the fold is correct, the move cell is unconditioned on who played it | NEEDS INVENTOR DECISION: MOVE cell conditioned on ΔElo resembles withdrawn context dimension. |
| #935 | chess reads: enforce command-count budgets and collapse player detail to one typed operation | ChessPlayerAsync fans out five parallel commands. |
| #936 | chess PROPOSE: coarse native search ABI and bounded multicore scheduler | Coarse native PROPOSE ABI. |
| #938 | chess Play/API: make state, history, outcome, and tenant provenance authoritative | Forgeable outcomes / tenant provenance. |
| #939 | EPIC: chess operational closure — routes, truth, lifecycle, throughput, and live proof | Chess operational closure tracker. |
| #1397 | Chess standing replay must use canonical event-time rating periods, never ingest/file/worker order | Rating periods from event time; partly fixed. |
| #1398 | chess identity lookup: exact usernames must not fuzzy-search or auto-promote same-name profiles into identity facts | Exact username identity; mostly fixed; provenance repair left. |
| #1419 | chess: material as deterministic baseline; typed structural/evidence planes enter the common coupled forward program | Material floor + typed planes in common forward program. |
| #1424 | chess: pinned official-source Stockfish as calculation provider, source corpus and fixed comparator | Stockfish as pinned calculation provider. |
| #1430 | chess: Stockfish census must dedup by canonical calculation, not amplify one engine verdict per LINE | One calculation must not become N testimonies. |
| #1448 | chess: OUTCOME rating/eff_mu scale explosion corrupts Player standings and server-side ranking | Billion-scale OUTCOME origin undiagnosed; no multi-period oracle fixture. |
| #1503 | chess ingest/readback: preserve every available source clock, annotation, and game metadata plane | Lichess export requests no clocks/evals; annotations via calculation qualifier, not markers. |

**chess (F)**

| Issue | Title | Why |
|---|---|---|
| #575 | chess: FEN/board state must enter the same canonical forward/query program | FEN resolves to canonical chess content through the common program; depends on #1720. |
| #833 | chess: compose UCI/play through the common COUPLE forward program, not root-only steering | Chess through the common program with typed planes. |

**code construction**

| Issue | Title | Why |
|---|---|---|
| #894 | Structural code construction: grammar/AST reuse, toolchain witnesses, and repair trajectories | Structural AST construction with toolchain witnesses. |

**code/D**

| Issue | Title | Why |
|---|---|---|
| #593 | code semantics: tree-sitter gives syntax, not resolved symbols — need an LSP-backed decomposer for real IMPORTS/EXTENDS/IMPLEMENTS/type relations | Resolved symbol graphs (LSIF/SCIP) as a format provider; retire RepoDecomposer PRECEDES/IS_TYPED_AS testimony. |

**code/vocabulary**

| Issue | Title | Why |
|---|---|---|
| #504 | ingest: decide the git-lane relation ledger (reuse vs new) BEFORE codegen — one reseed should pay for it | NEEDS INVENTOR DECISION: git-lane relations reserved; respect one relation per meaning and highway budget. |

**knowledge authority**

| Issue | Title | Why |
|---|---|---|
| #1425 | Billing + entitlements: knowledge packages and preflight measured compute | Packages; knowledge/authority/compute separation. |
| #1708 | P0 knowledge authority: packages, capabilities, effective-mind scoping, and Red/Blue/Judge enforcement | Packages/capabilities; authority before COUPLE. |

**perfcache**

| Issue | Title | Why |
|---|---|---|
| #1711 | P0 compositional perfcache lattice: modular mmap profiles, higher-tier ROMs, and cross-modality reuse | Perfcache lattice; patch size hard-coded. |

**perfcache/identity determinism**

| Issue | Title | Why |
|---|---|---|
| #49 | Cross-verification — perfcache/DB/codepoint generation parity | Exhaustive parity between the DB Unicode generation and the perfcache/native codepoint table (identity, coord, Hilbert) is still valid under spec 33. No verify-perfcache target or parity gate exists in the Justfile or workflows. |

**substrate-law (identity)**

| Issue | Title | Why |
|---|---|---|
| #506 | substrate-law: audit the whole SQL/native surface for Rule #1 OR-combined order-sensitive/insensitive metrics | Coordinate equality never identity; re-cite spec 05 Rule #1. |

## 5. P3: later or peripheral

**D**

| Issue | Title | Why |
|---|---|---|
| #806 | format-native metadata extractors: Gutenberg headers, EPUB/OPF, PDF-XMP, ID3, EXIF — feed the work node | EPUB/PDF/ID3/EXIF metadata providers. |

**D/documents**

| Issue | Title | Why |
|---|---|---|
| #807 | acceptance: prove editions collide, formats converge, and titles render — as tests, not as claims | Format-crossing work convergence; blocked on #806/#799. |

**E (export operators)**

| Issue | Title | Why |
|---|---|---|
| #376 | Foundry: geometric attention/positional operators (angular+Frechet head, hilbert/trajectory positional encoding, Voronoi MoE routing) | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). Needs an inventor decision. Export is a recipe over standing, geometry and selected operators (spec 12) but must load and pass in an external runtime (spec 09); custom operators (Fréchet head, Hilbert positional encoding, Voronoi MoE) are valid only where the target format and runtime can express them. |

**E (export validation ops)**

| Issue | Title | Why |
|---|---|---|
| #111 | Vendor + verify llama.cpp as the external validation harness for exported GGUFs | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). An external-runtime harness exists (check-model-proof-prerequisites.py, verify-model-behavioral.py) but uses an unpinned llama.cpp build under /data/archive/llama-workspace; external/ has only CMakeLists.txt and there is no .gitmodules entry. Only reproducibility pinning remains. |

**E (export)**

| Issue | Title | Why |
|---|---|---|
| #272 | Export metadata must be recipe-driven — WriteGgufMetadata is a hardcoded llama template | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). Still true: WriteGgufMetadata hardcodes 'general.architecture'='llama' and llama.* keys, and HfToGgmlName is a C# switch, now at FoundryCommands.cs:2168/:2249. Spec 12 requires a declared export recipe, and the ownership law puts encoding in native code. |
| #500 | engine: move FoundryExport/FoundryCommands transcendental math into engine/synthesis | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). 47+41 C# Math.* sites in Foundry; math belongs in native. |
| #514 | foundry: wire volatility, source-trust class and raw outcome polarity into the generation planes (P5 residual) | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). Consume volatility in export planes; drop second trust weighting (double counts). |
| #515 | foundry: tier-scheduled layer operators — replace all-ops-every-layer with the resolution ladder | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). NEEDS INVENTOR DECISION: derived layer/operator schedule (spec 09/12); rebase off archived specs. |
| #518 | foundry: re-measure FillGateBanded band membership now that entities.highway_mask is populated | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). FillGateBanded perfcache vs highway_mask measurement. |
| #620 | P1 foundry: move per-element linear algebra to native + bound plane top-k | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). PPMI and randomized SVD in C#; move native. |
| #955 | FoundryDefaults' 25 tuned scalars are a hyperparameter table with no provenance | After the forward program works (inventor: export generates every slot by querying standing; if the reads are wrong the export is muddied). FoundryDefaults tuned constants without provenance. |

**F (A* operator)**

| Issue | Title | Why |
|---|---|---|
| #353 | astar_path: calibrated (not just admissible) geometric A* heuristic | astar_geo_heuristic is still h = angular/ASTAR_PI (astar_path.c:196-219). A tighter admissible heuristic from reproducible calibration is a valid, peripheral operator improvement. |

**H/D (encyclopedic sources)**

| Issue | Title | Why |
|---|---|---|
| #372 | Uncracked-List F — Encyclopedic lane (proper names, Wikidata-shaped decomposer) | Wikidata, Wikipedia, GeoNames and Natural Earth are acquisitions beyond the priority list; capital(France) in the next slice reads from OEWN/OMW/CILI. The requested 'Wikidata-shaped decomposer' is superseded: it must be a recipe over the JSON/RDF provider (D), one witness per dump. |

**H/audio**

| Issue | Title | Why |
|---|---|---|
| #803 | Tatoeba audio: admit recordings as first-class structures and bridge them to text through attributed source evidence | Tatoeba audio modality. |

**J**

| Issue | Title | Why |
|---|---|---|
| #991 | gate: a STABLE function invoked inside WHERE has no check — the rule is prose only | STABLE-in-WHERE gate still open (SqlVolatilityGateTests). |
| #1175 | LapSight: decomposer amplification, convergence, skew, and acceptance gates | Per-source amplification/census receipts; re-base after reseed. |
| #1674 | Make CI/CD dependency-aware, daisy-chained, and operator-friendly | Dependency-aware CI. |

**J/chess**

| Issue | Title | Why |
|---|---|---|
| #747 | regress fixtures hand-transcribe the write path — generate them from the emission path instead | chess_read.sql hand-staged fixtures. |

**J/ops**

| Issue | Title | Why |
|---|---|---|
| #792 | ops: empty / thin substrate must fail loud — smoke gate, not silent skip | Substrate census guard. |

**K (parked)**

| Issue | Title | Why |
|---|---|---|
| #371 | Uncracked-List E — WSD witness loop (living priors) | Parked (inventor, 2026-09-25). Reframe as frayed-edge/gap detection, a 'periodic table of knowledge' where the structure of known knowledge exposes its gaps: the Gödel incompleteness lane (#1726) with the fray census (#1048). It is not the engine attesting its own disambiguations. |

**SQL/perf**

| Issue | Title | Why |
|---|---|---|
| #1135 | SQL architecture: land the corpus audit ratchet and execute the measured planner/duplication queue | SQL audit ratchet valid tech debt; much converse SQL moves native under #1720. |

**UI/product**

| Issue | Title | Why |
|---|---|---|
| #1404 | Product: universal tier/entity leaderboards with browse, rank, and drill-down navigation | Leaderboards with declared arena; UI peripheral. |

**app debt**

| Issue | Title | Why |
|---|---|---|
| #499 | area:app: split the monoliths — FoundryCommands 2218 / FoundryExport 2059 / CpuTopology 1517 / ModelTokenEdgeETL 1203 LOC | Foundry/CpuTopology monoliths; do with #500. |

**benchmarks**

| Issue | Title | Why |
|---|---|---|
| #1429 | Preserve whole-UD-Treebanks ingest throughput as calibration evidence | Legacy UD throughput; historical only. |
| #1436 | Turn benchmark evidence into a versioned suite and measure serviceable + saturation scaling | Serviceable-capacity proof. |
| #1438 | Register existing perf probes under benchmark-suite execution boundaries | Register perf tests in benchmark profiles. |
| #1561 | Benchmark query/cognition by hops, fanout, responders, and preflight-vs-actual work receipts | Query counters on populated runtime. |
| #1710 | Benchmark complete application mutation: structural reuse to exact full-repository realization | Application mutation benchmark. |

**billing**

| Issue | Title | Why |
|---|---|---|
| #791 | Epic: monetization / sell pillar — Stripe self-provision, API keys, tenant isolation, metering | Monetization epic. |
| #1428 | Inventory reusable billing/account plumbing separately from placeholder credit semantics | Billing placeholder inventory. |

**billing/auth**

| Issue | Title | Why |
|---|---|---|
| #1012 | billing: the tenant is an unauthenticated free-text header, nothing meters, entitlements are always empty, and keys/operator have no UI | Authenticated tenant identity, metering, quote gating. |

**billing/perf**

| Issue | Title | Why |
|---|---|---|
| #1431 | Preserve EXPLAIN/ANALYZE and execution receipts for resource-plan calibration | Preserve plan/execution receipts. |

**chess**

| Issue | Title | Why |
|---|---|---|
| #495 | chess: live end-to-end verification debt (FIX 2/4/5/8/9, chess-books ingest, cutechess auto-ingest) | Process/proof debt without acceptance; rewrite as a whole-slice live chess read. |
| #497 | chess: process-wide live-game gate and sync-over-async serialize parallel Lab games | Process-wide write gate + sync-over-async in Parallel.For. |
| #546 | chess: re-seed openings — LINE entities + clear pre-LINE / games=4 residue | Openings reseed ops debt; include source-id/trust-class fixes. |
| #578 | chess: ChessCompose.Gate global lock serializes position-id composition across all concurrent games/requests | LaplaceCoreGate lock; profile first. |
| #834 | chess: drive/measure protocol — use observational pass (fold+openings+explore), not Stockfish-shaped cutechess defaults | Measurement protocol / lab guide. |

**chess E2E**

| Issue | Title | Why |
|---|---|---|
| #934 | chess E2E: assert HTTP outcomes, empty-corpus semantics, and non-recording Play/Lab flows | Browser/HTTP contract tests. |

**chess Lab UI**

| Issue | Title | Why |
|---|---|---|
| #940 | chess Lab SSE: broadcast sequenced events; shared Channel readers currently steal from each other | Shared channel reader. |

**chess ops**

| Issue | Title | Why |
|---|---|---|
| #937 | chess control plane: bound inputs, make admission atomic, and reap Lab/training/session state | Non-atomic admission. |

**chess perf**

| Issue | Title | Why |
|---|---|---|
| #839 | chess compose: 14.3 µs per ply is ~40x off — two board clones, a 200-char string and 35x over-generation, per ply | Board clone per ply; target native compose. |

**dataset acquisition**

| Issue | Title | Why |
|---|---|---|
| #505 | campaign R5: acquire NCVEC Technician 2026-2030 + General 2023-2027 question pools | Ham radio pools absent; outside priority list. |

**dataset estate**

| Issue | Title | Why |
|---|---|---|
| #1473 | dataset estate: acquire hate, toxicity, bias-contrast and prosocial corpora with usable provenance | Safety/toxicity corpora acquisition. |

**engine**

| Issue | Title | Why |
|---|---|---|
| #467 | engine: decide the fate of karcher_mean / log_s3 / exp_s3 — test-only callers today | NEEDS INVENTOR DECISION: karcher_mean now has a non-test caller; log/exp_s3 test-only. |

**extension debt**

| Issue | Title | Why |
|---|---|---|
| #465 | extension: expose math4d_distance_sq as a scalar; retire laplace_l2_sq | math4d_distance_sq vs laplace_l2_sq duplicate distance definitions. |

**index/perf**

| Issue | Title | Why |
|---|---|---|
| #168 | Story 1.14 — laplace_btree_hash128_ops opclass + benchmark vs stock bytea ops | No hash128_ops opclass source exists. A deferred access-path optimization with no conflict with current law. |
| #169 | Story 2.15 — laplace_gist_s3_ops opclass + benchmark vs gist_geometry_ops_nd | No gist_s3_ops source exists. The body already limits it to an access path, not a semantic nearest-neighbour mechanism, which is consistent with law. |
| #170 | Story 2.16 — laplace_sp_trajectory_ops SP-GiST opclass for trajectory prefix-match | No sp_trajectory_ops source exists. Needs re-scoping to the current trajectory physicality layout, since PRECEDES/CONTAINS are trajectory facts, not stored testimony. |

**machine-cost**

| Issue | Title | Why |
|---|---|---|
| #1709 | P0 machine-cost derivation: control/data-flow execution counts to target microarchitecture cycles | Machine-cost derivation. |

**native determinism**

| Issue | Title | Why |
|---|---|---|
| #164 | D.6 — Determinism ctest — same output across thread counts | engine/dynamics/tests/test_procrustes.cpp exists, but no dynamics test checks TBB thread-count invariance. Thread-count invariance of canonical output is still law. |

**native determinism/perf**

| Issue | Title | Why |
|---|---|---|
| #121 | Epic D — MKL / Eigen / Spectra / TBB integration + determinism | Ownership law: native MKL/TBB, and worker count must not change canonical state. MKL_CBWR and the ISA option have landed (engine/dynamics/src/init.cpp); the epic stays open only for the missing #164/#165 gates. |

**native hygiene**

| Issue | Title | Why |
|---|---|---|
| #627 | P2 native: magic constants -> named/GUC, long-function table dispatch, redundant buffers | rank default 0.09, respond_routed strcmp chain, per-tensor memcpy remain. |

**ops**

| Issue | Title | Why |
|---|---|---|
| #1376 | ops: retire drifting Justfile/Batch/YAML implementations behind one `laplace ops` control plane | One ops control plane; peripheral. |

**ops/CI**

| Issue | Title | Why |
|---|---|---|
| #165 | D.7 — CI verification of MKL/TBB linkage | No workflow has an ldd or symbol check on liblaplace_dynamics/core linkage; still valid as an ops gate. |
| #1373 | EPIC: operational pipeline consolidation — one category surface, one executor, one receipt | Ops consolidation; re-base on current authority (OPERATIONS.md archived). |
| #1374 | ci: replace duplicated seed workflows and path resolvers with one manifest-driven seed pipeline | Seven seed workflows remain; not blocking. |
| #1377 | ci: replace persistent shared-workspace coupling with immutable build receipts and runner roles | Immutable build receipts. |
| #1378 | ci/ops: every workflow must emit one authoritative operation receipt and Actions summary | Authoritative operation receipts. |

**ops/CI determinism**

| Issue | Title | Why |
|---|---|---|
| #50 | Story 3.12 — Cross-machine determinism CI check | A byte-identical perfcache across machines is still a valid ops gate. The body cites the legacy 'laplace seed-unicode / UnicodeDecomposer' path; Unicode is now recipes/unicode, so the DB side must be re-pointed. |

**perfcache/ops**

| Issue | Title | Why |
|---|---|---|
| #382 | Perfcache codegen Wave 1 remainder: C# valet routing + highway source-hash header parity | A no-op gate on the highway blob's source hash, matching the T0 blob, fits spec 33. The 'C# valet' may only orchestrate the native/CMake codegen, not encode the blob. |

**product/K**

| Issue | Title | Why |
|---|---|---|
| #1421 | product: Knowledge Arena — deterministic games over pinned Laplace worlds with receipted hops/fanout | Knowledge Arena game firmware. |

**schema hygiene**

| Issue | Title | Why |
|---|---|---|
| #957 | purpose-schema migration is unfinished: wrappers, homeless families, and a schema shadowing a table | Homeless families, schema naming, stale header. |

**web/UI**

| Issue | Title | Why |
|---|---|---|
| #1450 | web: add in-place fullscreen/workbench expansion for Glome and other dense inspection surfaces (no redirect) | In-place fullscreen. |

## 6. Absorbed: do the work in the owner

**Owner #1719** (23)

| Issue | Title | Carry |
|---|---|---|
| #114 | Scoped-pour validation: a source/context-filtered export differs from the default pour in the expected direction | #1719 E7 owns validating a scoped export per spec 09 (held-out semantic and source-ablation tests). No ablation or scoped-pour check exists in model-synthesize-ci.sh or verify-model-behavioral.py, although FoundryCommands has --scope-source. |
| #374 | Uncracked-List I — Argentina gate (campaign acceptance test) | Reading model-source-only evidence through the substrate is #1719 E6 (source-scoped inspection through the forward program); its dependencies #368-#370 are superseded or stale. |
| #383 | ArchitectureProfile: QK-norm, decoupled head_dim, prefix-drift pattern matching (near-term model-family coverage gaps) | ArchitectureProfile hardcodes families; E2 shape recognition owns QK-norm/decoupled head_dim; unknown family must hard-fail. |
| #384 | ArchitectureProfile: MoE storage conventions, MLA (DeepSeek), DSA lightning indexer, FP8 block-quant, MTP layer count (DeepSeek/advanced-MoE coverage) | MoE conventions, MLA, DSA indexer, FP8 scales, MTP, tied/null lm_head recognized structurally under E2. |
| #385 | Model-lane perf: owed measurements (per-phase split, pack-throughput microbench, TinyLlama first timed run, VTune pass, readback-at-scale) | Owed measurements of legacy model lane; per-phase receipt + VTune of TinyLlama ingest after E lands. |
| #477 | model-lane: ModelConfigReader does not parse rope_scaling | rope_scaling unparsed; record as source-scoped structure. |
| #478 | model-lane: sliding_window / chunked-attention scalars are not witnessed | sliding_window/chunked attention unparsed; record as source-scoped structure. |
| #479 | model-lane: SafetensorsContainerParser ignores model.safetensors.index.json and silently unions shards | Shard index never read; E1 provider reads index, hard-fails on missing shard. |
| #482 | model-lane: fused-QKV (c_attn/query_key_value), fused gate_up, and GPT-2 Conv1D transpose unhandled | Fused QKV / gate_up / GPT-2 Conv1D transpose recognized by shape under E2. |
| #483 | model-lane: native GELU kernels (exact-erf, tanh, quickgelu) | Only SiLU-gated and erf-GELU kernels; add tanh-GELU/quickGELU if E's FFN contract needs them. |
| #485 | model-lane: verify the WordPiece path through LlamaTokenizerParser vs MiniLM vocab.txt | WordPiece gate under E3; continuation pieces stay source-local. |
| #524 | read-side: derived word->synset->word projection so hub synonymy reaches 1-hop planes | Synonymy is a same-language traversal through the shared identifier (word → identifier ← word), the sibling of translation; an export plane may derive it, but it is never stored as testimony. |
| #537 | model-lane: prove the quantized-container refusal path — GGUF/GPTQ/AWQ must hard-fail ingest | E1 provider must refuse quantized/packed containers. |
| #538 | model-lane: TOK-BPE encode-replay gate — re-tokenize from witnessed vocab+merges and prove id-for-id agreement | E3 id-for-id tokenizer re-encode gate. |
| #539 | model-lane: tokenizer sidecars unread — special_tokens_map.json / added_tokens.json / tokenizer_config.json never witnessed at ingest | E3 witnesses special/added tokens and tokenizer_config via JSON provider. |
| #793 | Campaign: model-lane honesty sweep (pillar 2 witnessing defects) | Model lane epic; model_forward verification superseded. |
| #923 | Model lane: source-scoped circuit comparison cube and cross-architecture correlation | Circuit identity omits the model; cross-source queries into E6. |
| #927 | Model consensus: one pooled heterogeneous-source forward pass with A/B/A+B ablation | Pooled model consensus = E6. |
| #928 | Model export: deterministic source-scoped and pooled constructions with provenance receipts | Deterministic export = E7. |
| #1034 | ffn_edges O-normalisation is safe only because its consumer thresholds — undocumented coupling | ffn_edges O-normalization + constant theta; E4 declared significance replaces constant floors. |
| #1054 | export operator mapping: finite target tensor roles over an extensible Laplace plane/ISA registry | Export operator mapping is E7; remove duplicate contractions; fix >300-tensor unfilled bug. |
| #1111 | Nested text_config models parse to all-zero scalars and deposit FALSE recipe attestations | Nested text_config not descended; never deposit zero scalars. |
| #1344 | model lane: live substrate again contains 1.2 GB of forbidden model geometry payload | No raw values in any form; zero-payload gate on the new E5 path. |

**Owner #1720** (21)

| Issue | Title | Carry |
|---|---|---|
| #354 | Consolidate the four independent native walk/traversal engines (steered_walk.c, walk_continuations, walk_strongest, walk_branches) | Four engines still exist (steered_walk.c, trajectory_generate.c, walk_strongest/walk_branches in generate_walk.c). Walks are operators inside the program; #1720 rebuilds the native forward program, which settles which traversal operators remain. |
| #359 | SPEAKING: language-agnostic discourse/sense evidence must participate in whole-prompt COUPLE, not an English topic gate | #1720 owns typed whole-prompt COUPLE and removal of English-specific matching (prompt_coherence.c labels, English templates); this issue restates spec 36's no-topic-before-coupling rule. |
| #409 | SPEAKING: walk_text / trajectory_continuations times out at scale | Chat path timeout on legacy walk_text lane; F acceptance: one-word chat inside budget with trace, no substrate_unavailable misreport. |
| #459 | read-side native lift: rank_edges(subject_ids, types, k) — collapse four duplicated ranking cores | salient_facts/top_relations/evidence_receipt separate SQL cores; one native ranking primitive returning typed standing. |
| #464 | read-side: edge_strength(subject,type,object) + batch form; bless label() or render() as the one renderer | label*/render* families both installed; one REALIZE authority; edge_strength scalar conflicts with typed standing. |
| #517 | read-side: native SPI adjacency walk to retire relation_rank_resolved (~27us/call) | relation_rank_resolved per-row calls; native COUPLE reads rank from ROM in-loop. |
| #617 | read-side: inlining audit — SET search_path on LANGUAGE sql functions in per-row/per-step hot positions | SET search_path on legacy readers replaced by native forward program. |
| #752 | substrate: tier-collision seam — a surface's sense set unions its tier-0 Codepoint lineage with its word senses | RESOLVE/COUPLE must select the meant physicality by tier (letter A vs article). |
| #756 | read-side: questions route themselves — relation-name attestation + infer() rel_type wiring | Questions route themselves = ROUTE; NOTE no capital relation in the registry. |
| #785 | READ: held-out sense/orientation failures must be solved by whole-prompt COUPLE, not an early elector | No early elector; held-out suite (glacier, pawn, bank, fork, dog) as #1720 acceptance. |
| #804 | realization: `render(id) -> text` is one projection; callers need declared target form/recipe and truthful abstention | Target-aware REALIZE with truthful abstention. |
| #990 | recall: the commonsense corpus is folded and unreachable — recall_interaction_response is the one responder the C dispatcher never routes to | Commonsense responder missing; forward program must reach commonsense relations once ATOMIC/ConceptNet are recipes. |
| #1009 | query shapes: generate/walk/beam/neighbors present a different layer as inference instead of degrading like complete does | generate/beam/neighbors/walk shapes present layers as inference; re-express as operators. |
| #1011 | chat: the endpoint returns laplace:{} on the content chunk, so every reply's "μ and witness counts attached" is unbacked | chat/completions empty laplace:{} receipt; must carry typed standing per claim and path ids. |
| #1016 | translate: HTTP lane reports 'no witnessed fr translation' for words the MCP lane translates (ISO code never resolved to language name) | translate lang parameter must resolve via ISO 639 registry; translation is traversal. |
| #1018 | chat/completions answers about the synset head lemma instead of the queried word (dog→"Chase", Sherlock Holmes→"Pi") | Answers about synset head lemma; REALIZE must realize the queried surface. |
| #1047 | SQL surface: no declared cardinality contract — 152 of 213 read-side functions are scalar-in, and every measured hot path is a scalar called in a loop | Read-path scalar loops; set-in/set-out cardinality contract. |
| #1099 | Typed relations are stranded in HAS_DEFINITION gloss text — 'capital of France' is not an election defect | capital(France) fact only in gloss text; NEEDS INVENTOR DECISION on mechanism (calculated gloss analysis vs a stating source). |
| #1178 | Readers: consume normalized senses, occurrences, collections, and trajectories without compatibility edges | Readers over senses/alignment consensus replaced by direct bindings; keep bank/fork fixtures. |
| #1401 | P0 forward-pass integration: whole-observation coupling over physicality, evidence, calculations, and discourse | Whole-observation coupling before routing; authority before COUPLE. |
| #1478 | Forward pass: semantic responders must enter the coupled frontier before proposal/steering truncates them | Semantic responders enter frontier before truncation; A/B fixture. |

**Owner #1717** (11)

| Issue | Title | Carry |
|---|---|---|
| #259 | Refactor orchestration boundaries across Laplace: repeated algorithmic work belongs in coarse native/set operators, not C#/SQL RBAR | This is the ownership law itself (native does repeated work, PostgreSQL stores, SQL/C# orchestrate; RBAR and per-row SPI are defects). Its ingest half is #1717 C and its query half is #1720 F. |
| #417 | refold-on-reingest: novelty gate must reach the consensus fold | No-op re-ingest must not refold or change witness_count; covered by C's in-memory fold + per-witness replacement. |
| #429 | INGEST: hot-path existence probe cost under pure-COPY law | attestations_exist_bitmap probe dedupe; C replaces with native reduce + key conflicts. |
| #910 | Tier descent pays per-node P/Invoke under a global lock; apply re-parses native PGCOPY blobs — two native API additions delete both | Per-node P/Invoke and managed PGCOPY re-parse. |
| #952 | ingest: FrameNet is 37x slower per entity than unicode — re-observation re-probes across batch boundaries | FrameNet probe baseline is #1717's before-number. |
| #964 | fold: cili folds at 931 cells/s vs unicode's 30,347 — 33x collapse, and it is 96% of the foundation's cost | highway_mask_deposit per-chunk pass and SPI refold replaced by native fold; advisory-lock convoy must not survive. |
| #967 | ingest: finish generic apply concurrency and prove physical-plan invariance under the shared pipeline | Generic apply concurrency; invariance owned by #1443. |
| #1292 | Apply durability: journal-visible fold completion + automatic scoped refold (evidence durable ≠ consensus current) | Fold-after-journal patch removed by C's in-memory fold; run must not report complete while fold owed. |
| #1328 | ingest: a cancelled ingest leaves its partial evidence folded into consensus, and nothing marks the substrate as holding a truncated corpus | Truncated source repaired by per-witness eviction; mark incomplete witness. |
| #1372 | LapSight: consensus-drain publishes no live owner, progress, or stall evidence | consensus-drain opacity from async fold; new fold must publish live progress. |
| #1714 | Bulk load performance: computed columns, per-type/per-leaf SQL, upserts and flat probes on the ingest path | Bulk load defect list carried into #1717. |

**Owner #1718** (7)

| Issue | Title | Carry |
|---|---|---|
| #418 | format-aware tier composition for .md/.rst/.html; Tree-sitter optional, not mandatory router | TreeSitterTextAdapter still claims json/xml/csv/tsv; narrow it and replace the gate test under D. |
| #929 | Seed capability manifest: every dataset declares tiers, relations, trajectories, and product contribution | Per-source capability declaration = recipe; drop completion markers. |
| #1010 | text/document ladder: preserve line/paragraph/page/section/chapter structure without salting identity with tier | Document structure roles via format providers; Line_Break is not an observed line. |
| #1041 | P0 admission: finish reference-vs-content classification across every source field | Field-disposition law is the recipe layer; coverage receipt, unresolved never Content. |
| #1045 | EPIC: recipe-driven ingest — source providers recover structure; one shared machine owns canonical admission | Recipe-driven ingest epic owned by D; keep boundary law as acceptance. |
| #1177 | EPIC: ingest/decomposer normalization — one content law, invariant physical plans, measured admission | Shared admission machine epic = D; carry physical-plan invariance. |
| #1403 | P0: enumerate complete physical source estates and execute every selected artifact through generic ingest | Complete artifact inventory and generic ingest = D. |

**Owner #1722** (7)

| Issue | Title | Carry |
|---|---|---|
| #365 | ConceptNet: re-seed on hart-desktop (0 attestations currently — prior killed refold never completed, likely lost in cluster rebuild) | #1722 H owns ConceptNet onto a recipe and seeded. The check here uses laplace_hash128_blake3('substrate/source/ConceptNet/v1'), a source id the witness-identity law supersedes. |
| #761 | campaign: seed the corpus through finished lanes, re-scoring the eval harness per wave | Seeding in dependency waves; score waves by live reads. |
| #1153 | P0: preserve native semantic-source fidelity across decomposers | Per-source fidelity losses become recipe acceptance tests (VerbNet fn_mapping, FrameNet FE scoping, PropBank roleset scoping). |
| #1180 | OpenSubtitles: keep batching, language, and source ordinals out of content identity | OpenSubtitles alignment identity salted with schema/pair/ordinals; recipe acceptance: identity only from ordered constituents. |
| #1471 | dataset estate: replace legacy semantic mappings with current attributable releases | Replacement semantic mappings; vault dispositions carried. |
| #1472 | dataset estate: refresh mutable corpora and retain complete selected sidecars | Refresh mutable corpora; ATOMIC10x separate lower-trust witness. |
| #1713 | Admit the .refresh-20260903 source estate as selected source generations | Refresh estate admission; add GeoNames/NaturalEarth/Safety/Lichess to #1722; spine items to #1718. |

**Owner #1726** (5)

| Issue | Title | Carry |
|---|---|---|
| #355 | doc 15 B3 — Behavioral harness as witness (standing SQL-engine test suite deposited as attestations) | 'Evaluation IS ingestion' self-witnessing is the Gödel/OODA loop that #1726 must reconcile with Laplace-Refactor's authority stack. The proposed 'substrate/source/BehavioralHarness/v1' string id is superseded; a witness is SourceWitness.Id. |
| #356 | doc 15 B4 — Walk-policy rating (decode policies as content-addressed, Glicko-rated entities) | Content-addressed decode policies rated by outcome are #1726's personality firmware: content-addressed policy over ROUTE/STEER/SELECT, witnessed and rated like a source. |
| #357 | doc 15 D — Loop-closure metrics (feedback-to-next-walk delta, depth-k accuracy, latency budget) | Loop-closure metrics measure the Gödel/OODA feedback loop #1726 owns; depends on #355, also absorbed there. |
| #379 | doc 18 Q6 — Echo-loop guard for the generation corpus (self-improvement vs self-contamination, same loop) | Whether generated output becomes corpus or testimony, and at what trust, is #1726's self-witnessing reconciliation; the n-gram path it names still exists (trajectory_generate.c). |
| #823 | docs: substrate as digital processor — Gödel firmware / ISA framing (deterministic transformer slots) | ISA-as-processor framing is an input to #1726. |

**Owner #1723** (3)

| Issue | Title | Carry |
|---|---|---|
| #399 | highway_mask grain: relation-TYPE bits vs value-grained (reseed-class decision) | Highway stays type-grained; value filtering via entity OR-masks and qualifier masks; record the ADR under I. |
| #1038 | Vocabulary/registry entities must have declared identity/physicality law, not masquerade as tier-2 composed content | Registry entities never rows; gate: no vocabulary rows or fake trajectories for registry keys. |
| #1133 | substrate: canonical modality registry + derived modality masks (routing only, never identity) | Modality registry + derived OR-masks is I's work. |

**Owner #1562** (2)

| Issue | Title | Carry |
|---|---|---|
| #1048 | ops.fray_census(): typed enumeration of every break in the composition invariant, with per-run delta as an ingest gate | Fray census as set-based per-run delta under #1562. |
| #1072 | ops.render_trace shipped to main having never returned a row | render_trace never run on data; run its acceptance on next seed. |

**Owner #1725** (2)

| Issue | Title | Carry |
|---|---|---|
| #924 | EPIC: conversation, structural software construction, and heterogeneous-model consensus | Finish-line epic now #1725; drop replay children. |
| #1132 | EPIC: substrate cohesion — exact recursive laws, governed operations, and measured full-world admission | Cohesion epic superseded as organizer by #1725. |

**Owner #458** (2)

| Issue | Title | Carry |
|---|---|---|
| #460 | read-side native lift: taxonomy_closure(seed_ids, dir, depth) — replace 4 recursive CTEs | Remaining recursive closure is laplace_ancestry; same site as #458. |
| #461 | read-side native lift: resolve_path — unify relate_path's recursive CTE with astar_path.c | relate_path already native; unify with astar_path into one path engine under #458. |

**Owner #1721** (1)

| Issue | Title | Carry |
|---|---|---|
| #360 | doc 22 Phase D — Session as a TRAJECTORY entity (retire session_topics unlogged-table stopgap) | #1721 G lists converse.session_topics for removal (still read in chat.sql.in:259, :395); the proposed SubstrateChangeBuilder writer spine is legacy under C. |

**Owner #1724** (1)

| Issue | Title | Carry |
|---|---|---|
| #1315 | verification: green tests were not evidence — three load-bearing invariants had no guard that could fail | Test hygiene; extend mutation guards. |

**Owner #272** (1)

| Issue | Title | Carry |
|---|---|---|
| #273 | Move byte-BPE re-encoding + generation_config read out of WriteGgufMetadata (naming already native) | The call-site half of #272: delete C# WriteGgufMetadata and HfToGgmlName in favour of native bindings. The title is wrong that naming is already native; HfToGgmlName is still C# at FoundryCommands.cs:2168. |

**Owner #515** (1)

| Issue | Title | Carry |
|---|---|---|
| #516 | foundry: expose the per-layer operator schedule as a recipe knob (UI-specifiable) with a derived default | Declared schedule knob = recipe half of #515; body pasted from unrelated defect. |

## 7. Superseded: do not implement

| Issue | Title | Why, and any kernel carried over |
|---|---|---|
| #8 | Milestone: run an exported substrate GGUF in llama.cpp and judge it semantically | Epic framed as an 'AI<->DB codec' round trip (ingest TinyLlama checkpoint, export it back, chat). Law: export is a filtered snapshot of standing, not checkpoint reconstruction; acceptance is never a comparison with the original (roadmap law 15). Sub-stories #110 roundtrip.sh and #113 chat smoke assume that codec framing. |
| #264 | Recipe lane residual: RecipeInfo DTO + managed canonicalization (duplicate JSON parser is gone) | The remaining work keeps the managed CanonicalizeJson in LlamaRecipeExtractor.cs only so RecipeEntityId, a hash of the config file, stays stable. A config file is packaging, not content, and roadmap E lists the Blake3 recipe and tokenizer entities as bookkeeping to remove (#1721 G). |
| #350 | walk_branches: hilbert-band-scoped search for trajectory-ordinal continuity (currently opt-in, default-off) | Tunes an opt-in beam term in the legacy walk_branches engine whose per-candidate SPI containment probe (beam*3 per node) is the per-row SPI defect. Walks are operators inside the forward program, not the cognition (INVENTIONS #69; #1720). |
| #351 | walk_branches: generalize to the unfiltered-type case (recall_walk_response 'walk' mode still uses the old walk_strongest) | Enriches 'walk' mode of the legacy recall_walk_response reader (still calls walk_strongest) with a hand-curated default relation list. #1720 replaces these hand-rolled readers with the native forward program. |
| #363 | Drop dead attestations.highway_mask column (native tuple layout cleanup) | The column is not dead: since 4a0f83ff2 attestations.highway_mask carries the governed claim qualifier mask (AttestationRow.QualifierMask, OR-merged; qualifiers.toml). Dropping it contradicts 'variants are qualifier flags on the attestation'. |
| #364 | WSD sense-selection via frontier-queue backtrack semantics (0=backtrack, not abstain) | Picks a top-k sense and backtracks through the walk beam, electing a sense before whole-prompt coupling. Law: ORIENT resolves sense from the whole observation (spec 36; roadmap slice 'minute'); the sense mesh is gone. |
| #369 | Uncracked-List C — OODA fold of walked model-pair evidence | Lazily depositing model-pair evidence per walk touch re-scores pairs from retained model factors. Law: no retained weights or replay; roadmap E names 're-score pairs already in consensus' as a defect. |
| #370 | Uncracked-List D — Frontier-as-residual composition (deep-layer replay) | Acceptance is 'deep-layer replay' with >=90% top-1 attention agreement and next-token overlap against the original. Law: no weight replay; acceptance is never agreement with the original. |
| #373 | Uncracked-List G — Sentence-grain probes (positional heads) | Sentence-grain probes run sentences through the model's heads: prompting/replay. Law: no prompting or replay; head roles are recognized structurally as source-scoped coordinates (#1719 E2). |
| #433 | verified at-scale runs: UD, Tatoeba, chess-ANALYZE profile | (kernel to #1722) Proof runs of legacy UD/Tatoeba decomposers; H moves both to recipes; chess markers removed under G. |
| #457 | read-side: serving chain stacks non-inlined SQL functions — build native-SPI translate_to_fast | (kernel to #1720) Native translate_to_fast over synset_members mesh; translation is a traversal in the forward program. |
| #475 | model-lane: Tier-2 behavioral-fidelity gates against a llama.cpp reference | (kernel to #1719) Fidelity gates against the original model via llama.cpp; acceptance never vs original. |
| #476 | model-lane: implement the named ModelGate_* exactness gates | (kernel to #1719) Exactness gates from retired 27b plan (factor readback vs checkpoint); ModelGate tests gone. |
| #480 | model-lane: safetensors __metadata__ block is skipped, not witnessed | (kernel to #1719) Record __metadata__ as provenance testimony; provenance is not claims. |
| #484 | model-lane: witness generation_config.json decode scalars on the recorder side | (kernel to #1719) generation_config decode scalars parameterize running the model; decode policy belongs to firmware (#1726). |
| #487 | INGEST: analyzer marker grain — mid-batch crash still double-counts testimony | (kernel to #1721) Rests on AnalysisMarkerId/ANALYZED_AT markers being removed; carry crash/restart observation_count invariance to #1717. |
| #498 | ingest: GrammarRowReader.FeedChunkFields allocates a string[] per row for all tabular ingest | (kernel to #1718) Tuning legacy GrammarRowReader per-row string arrays; TSV provider under #1718 must stream natively. |
| #519 | foundry research: make the correction planes head-informative (apply the readout law to correction subspaces) | Research spike from archived spec 14; heads derive from spec 12 slot map. |
| #522 | foundry: synthesize selectors — EVOKES_FRAME/definition planes as QK, G-subspace bookkeeping | Frame/definition planes as QK selectors from archived spec 18; QK is typed COUPLE. |
| #523 | substrate-law: XPOS->UPOS is recorded as IS_A, but doc 18 calls that a recording defect (CORRESPONDS_TO) | (kernel to #1722) XPOS->UPOS as IS_A/CORRESPONDS_TO testimony; normalize at ingest via governed mapping in the UD recipe. |
| #542 | model-lane: HasBiases is one boolean per profile — cannot express per-projection bias presence (Qwen2) | (kernel to #1719) Patches ArchitectureProfile HasBiases; E2 recognizes biases per projection. |
| #543 | model-lane: Bert profile FinalNorm points at embeddings.LayerNorm.weight — embedding norm misfiled as final norm | (kernel to #1719) ArchitectureProfile FinalNorm mismap; E2 distinguishes by structural position. |
| #544 | model-lane: norm placement/topology (pre-LN / post-LN / sandwich / parallel) is not a witnessed field | (kernel to #1719) Framed as replay fidelity; norm placement recorded as structure under E2/E5. |
| #545 | model-lane: the phi profile is Phi-1/2 (mlp.fc1/fc2) — Phi-3 tensor names mismap under the same model_type | (kernel to #1719) model_type dispatch mismap; E2 never fails open on model_type. |
| #548 | ingest: XPOS tags are minted unnamespaced (bare NodeHash) while UPOS goes through the governed resolver | (kernel to #1722) XPOS anchors now OfCanonical string ids; NEEDS INVENTOR DECISION on XPOS (governed tagset registry vs UPOS+features). |
| #552 | engine: move recipe canonicalization to native, gated on proving byte-identical output | (kernel to #1719) Blake3(config.json) recipe id is bookkeeping; config enters as content via JSON provider. |
| #658 | voice: ORIENT can seed on a one-word translation lemma instead of the real gloss | converse_walk ORIENT over sense mesh; realization via bindings in query language (#1720). |
| #753 | coherence: sense election needs a salience prior — content-band mass comparator + HAS_SENSE_RANK at WordNet ingest | Early sense elector + HAS_SENSE_RANK at WordNet 3.0 ingest; lexicon sense order may become a qualifier/read-time salience. |
| #757 | read-side: port infer() to C — both directions, n-hop bias family, multi-step loop | Porting legacy infer() to C builds a second read path. |
| #802 | document layer: a document is a composition over content, not the file's content root | Requires plain .txt doc id != content root; contradicts single-child identity. |
| #805 | source kind: corpus / runtime / derived, attested at bootstrap — 'is UserPrompt ingested' is a category error | Sources testifying their own kind; source kind becomes a governed declaration. |
| #861 | Elector ranks on a language-specific word-order prior because the joint signals are inert | (kernel to #1720) Tunes single-topic elector's scalar key. |
| #865 | Containment prior alone over-rewards rare tokens; must be weighed against the fold | (kernel to #1720) One combined scalar elector key violates typed state. |
| #954 | WEIGHT: the sequence epistemology is undocumented and unratified | (kernel to #1720) FOLLOWS/CO_OCCURS_WITH Glicko lane forbidden; sequence statistics are calculations over trajectories. |
| #1015 | Model lane: the magnitude→Glicko conversion was deleted; witnessWeight is hardcoded to 1.0 | (kernel to #1719) Restore kind_rank x trust -> phi and score only existing pairs; rank out of standing, re-scoring existing pairs is a defect. |
| #1024 | Evaluate fp64 -> fp32 across the dynamics/synthesis GEMM path | (kernel to #1719) GPU FP32 wiring for model lane contradicts no-GPU finish line; carry CPU fp32 determinism study into E1/E4. |
| #1025 | torch cu126 ships no cp310 wheels; sm_61 absent from arch_list but Pascal runs anyway | torch/GPU wheel note; nothing to build. |
| #1046 | taxonomy: bubble_down(concept_id, lang_id, k, filters, temperature) — the missing trunk-to-surface projection primitive | (kernel to #1720) bubble_down assumes sense mesh + softmax temperature; REALIZE descent from identifier is F. |
| #1051 | wire the spectral organ to consensus: Laplacian eigenmaps over the testimony graph — belief-geometry, diffusion path cost, and the coherence half the walk engine never got | (kernel to #1720) Belief geometry from one scalar edge weight as walk cost; typed state stays typed; spectral ops only as declared ISA operators. |
| #1176 | Evidence: explicit promotion, eviction, and rebuild policy for occurrence-derived consensus | Promotes occurrence statistics into consensus; co-occurrence is a trajectory fact; translation is traversal. |
| #1387 | ops: scheduled maintainer automation must consume the repository-owned contract and fail on drift | Requires following archived docs/OPERATIONS.md. |

## 8. Stale: verify and close

| Issue | Title | Evidence |
|---|---|---|
| #112 | Model load test: exported substrate GGUF loads and generates in llama.cpp | The claimed missing exported-GGUF load path now exists: scripts/model-synthesize-ci.sh runs 'synthesize substrate', then loads the GGUF in llama.cpp via verify-model-behavioral.py with a semantic gate, wired into seed.yml. |
| #115 | CI: wire scripts/model-synthesize-ci.sh into .github/workflows/laplace.yml as a self-hosted job | Already done in a different workflow: seed.yml (self-hosted, laplace) calls scripts/model-synthesize-ci.sh in 'model' mode when synthesize_model is set (line 332), not in laplace.yml as the body assumed. |
| #367 | render_native: bulk-fetch render path (zero per-node SPI calls) for converse's serving hot path | converse_compose/converse_walk now render in one set through realize.render_text_batch. The only per-row render_text left is an English 'X is Y.' template in converse_facts.sql.in:78, owned by #1720's realization rebuild. |
| #368 | Uncracked-List B — Native SPI scorer (model_pair_score/model_row_topk) | model_pair_score/model_row_topk no longer exist (model_factor.sql.in DROPs both; model_factor.c exposes only testimony decode and trajectory bits). Its gate (SQL score = kernel within 1e-6, top-k, ArchitectureProfile) is also superseded by E2/E4. |
| #462 | read-side: reconstruct_content(id)->bytea, entity_form(id), decompose(text) missing from the SQL surface | reconstruct_content.sql.in exists; ContentRoundtrip.cs uses NpgsqlContentReconstructor. |
| #473 | model-lane: A2 while-hot single-pass analyzer stack — HilbertIndex written as default | ModelTokenEdgeETL now encodes HilbertIndex; cited lines gone. |
| #474 | model-lane: enforce recorder-first — refuse or auto-run the witnessed layer when layer_complete=False | layer_complete log-only path gone; completion markers are fake machinery (#1721). |
| #488 | model-lane: model_forward v0 was merged while labeled UNVERIFIED — verify it against a rebuilt substrate | model_forward v0 dropped ('decomposition, not a second inference runtime'); replay would break law anyway. |
| #501 | read-side: salient_facts 16-entry relation exclusion list should be driven from relation_types.toml bands | salient_facts rewritten over bindings/key_facts; key_facts flattening owned by #1720. |
| #512 | chess: build the board geometry ladder — square/piece S3 anchors, move transitions, position entities, packed game trajectories | Chess compose floor landed; only outcome-edge aggregation unverified (narrow issue if needed). |
| #521 | foundry: typed residual stratum allocation (S/W/C/F/G subspaces; heads as typed maps) | ResidualStrata.cs built and used. |
| #531 | billing: TryConsumeCredit FOR UPDATE + jsonb_set CTE belongs in an app.consume_credit() function | billing.consume_credit migration landed. |
| #547 | chess: game-tier mantissa-packed trajectory is not wired — EmitGame deposits a bare Document entity | PGN line trajectories exist via AppendLineTrajectory. |
| #550 | secrets: move the live Lichess token out of on-disk plaintext into the secrets flow | Lichess secret via repository secrets; operator to confirm old file removed and token rotated. |
| #577 | chess: seed-manifest hygiene — CORRESPONDS_TO/HAS_NAME_ALIAS emitted but undeclared; GAME_AT/GAME_AT_PLY/Chess_Concept declared but dead | Declared/retired in ChessSeedManifest; HAS_NAME_ALIAS emission to retire under #1723. |
| #588 | write path: 5 substrate perf/consolidation gates RED on main against the live DB — entity apply 126k rows/s vs 500k gate | Measured on replaced DB; writer throughput gates become #1717 acceptance. |
| #659 | generation/corpus: retire trajectory_pairs, bound corpus_ensure, stream_stats perf | Retired content lane dropped. |
| #660 | ops/ui: modality_counts has no 'documents' count — documents modality card can't render | modality_counts has documents column. |
| #754 | ingest: finish the document lane (Pillar 0) — stop borrowing UserPrompt; titles, source identity, license, typed trunk edges | Document lane done; but its blake3 source/trust ids move to #1721/#1715. |
| #764 | standardization: 358 functions, zero recorded dependencies — adopt BEGIN ATOMIC so PostgreSQL enforces the call graph | BEGIN ATOMIC conversion done except ingest_run_close. |
| #777 | ops: foundation seed owed — empty box here (THIN_SUBSTRATE); prove #776 on live ladder after seed | Orphan Unicode journal snapshot obsolete. |
| #800 | vocabulary: append EXPRESSES and HAS_TITLE bits — and reuse AUTHORED_BY/HAS_LICENSE/IS_TRANSLATION_OF rather than minting parallels | EXPRESSES/HAS_TITLE/AUTHORED_BY exist. |
| #801 | work node: declared canonical work recipe over title/author/edition evidence, separate from artifact bytes | WorkEntity implemented; move its attestations to witness/class trust. |
| #811 | EPIC: standardization — every substrate question is a typed operation; hand-written SQL is a missing tool | Generic op invoker on MCP and OpenAI surfaces. |
| #812 | surface parity: one 'op' invoker — every installed operation reachable from MCP and the OpenAI endpoint, gated | Op invoker done; confirm health tool timeout live. |
| #814 | the sql hatch must refuse what the typed surface can answer, and log every use as a gap report | Raw sql MCP tool removed. |
| #860 | SET search_path on 232 SQL functions blocks inlining and kills the index path | Only ingest_runs keeps SET search_path. |
| #871 | consensus_step_edge uses the both-directions OR predicate spec 37 blames for the 280s hang | step_edge direction-pinned arms. |
| #878 | converse_tiered: content regressed to pure topic echo; CREATE TABLE blocks read-only lanes | converse_tiered read-only; off hot path. |
| #908 | Surviving OR-joins in converse_tiered and consensus_peer; unfenced relation_in_family join qual | All three inlining items fixed. |
| #911 | plpgsql census follow-ups: per-tier re-unnest in entities_present_ordinals; ord assignment race in session_record_prompt | Native entities_present_ordinals; advisory lock. |
| #920 | MCP deployment: repair canonical launcher and prove live JSON-RPC product surface | MCP deploy probe exists; product/operator tool classification remains. |
| #932 | chess API: DI bootstrap and schema drift 503 the deployed play surface | highway_mask_deposit parity test; chess hosted runtime. |
| #956 | WITNESS: the turn close is implemented three times; MCP, OpenAICompat and CLI each roll their own | TurnCloser is the one implementation. |
| #958 | ingest: a local seed and a CI deploy share one PostgreSQL cluster with nothing preventing collision | pipeline.sh restart_postgres waits via wait-for-quiet-substrate.sh. |
| #989 | consensus can be deleted and decayed through the MCP op tool — api() is an unfiltered pg_proc scan | prune/decay retired; MCP op tool read-only with timeout. ops.api() schema-wide scan: inventor call. |
| #1008 | P0 identity/storage: one canonical entity per id; tier cannot be the uniqueness/partition authority | entities now PRIMARY KEY (id) HASH(id). |
| #1042 | text lane: finish UCD-authoritative whitespace classification; run-word emission is fixed | Whitespace classification now from generated perfcache flags. |
| #1043 | unicode authority: make complete pinned UCD XML the semantic property stream; remove split text/XML parsers | Unicode on recipes over flat XML; build ROM parser unification goes to #1718. |
| #1052 | storage integrity and claim-event structure: one entity per content id, explicit attestation state, set-based referential proof | entities keyed by id; claims-about-claims needs its own inventor decision if pursued. |
| #1057 | UD arcs land as type-level grammar statistics, not parses: no contextId, root arcs dropped, three divergent relation declarations, and a gate on an empty family | UdSentenceEmitter no DEP_* arcs; UD has a recipe and governed deprels; verify on first UD seed. |
| #1073 | consensus: 52.3% of rows land in consensus_rdefault — 'the partition IS the bit' does not hold for the foundation seed | consensus now HASH(subject_id) 64 partitions; no rdefault. |
| #1074 | model lane: the FFN contraction is linearized — gate_proj and the SiLU are dropped, and the error has never been measured | ffn_edges computes silu(gate)*up; FFN probe defect owned by #1719. |
| #1181 | structural.cluster: repair broken scalar/batch contract and replace hidden RBAR with a bounded core | structural_cluster_batch now set-oriented. |
| #1321 | consensus fold: the opponent is hardcoded to 1500 on every fold, so a cell forever replays the same match against a constant | Opponent rating now derived from witness weight. |
| #1362 | Foundry production callers still impose the retired 300-tensor ceiling | Foundry paths use ArchTemplateManifestMaterialize; no 300 ceiling. |
| #1409 | P0 ingest COPY protocol: packed filtered payload can exceed PostgreSQL CopyData message limit | COPY payload cap fixed (#1412). |
| #1423 | DB lifecycle: extension upgrade cannot rename generation.walk_continuations input parameters | walk_continuations param name kept. |
| #1441 | Separate unique-corpus makespan from independent-stream whole-machine throughput | Separate scale profiles exist. |
| #1445 | P0 chess FIDE acquisition: official search/roster must not complete with zero provider records | FIDE search via rating list; check legacy ChessGameFetcher.cs:426 path. |
| #1449 | web: glome visuals remain effectively planar and instance node spheres render black/invisible | Black spheres fixed. |
| #1451 | P0 performance/correctness: parallelize one semantic content DAG internally; files are not worker atoms | Dependency-frontier parallel composer landed. |
| #1479 | FIDE estate: stop reparsing the whole publication on browse; durable snapshot + bounded refresh | FIDE durable snapshot. |
| #1480 | Unify ingest orphan recovery after self-hosted runner shutdown | Orphan recovery via heartbeat journal. |
| #1505 | Explore graph: 3D nodes must not render black; unresolved realization must fall back to useful identity/content | Graph nodes render; fallback realization carried to #1720. |
