# Laplace invention catalog

This catalog names the invention mechanisms without reporting implementation status.
Architecture-as-built belongs in [ARCHITECTURE.md](ARCHITECTURE.md); active acceptance
belongs in GitHub; full annotated history is preserved in
[`docs/archive/catalogs/INVENTIONS_ANNOTATED_V1.md`](archive/catalogs/INVENTIONS_ANNOTATED_V1.md).

## Identity, physicality, and exact occurrence

1. **Content-addressed identity as the join.** Equivalent content from every source and
   modality converges without a separate entity-resolution model.
2. **Tier as altitude rather than identity.** The same content retains one id while
   participating at multiple compositional levels.
3. **Unicode/DUCET universal text floor.** Language and tokenizer boundaries sit above
   a deterministic codepoint/collation foundation.
4. **S³ Super-Fibonacci placement.** Canonical primitive placement provides uniform,
   deterministic four-dimensional coordinates.
5. **Hilbert content locality.** A locality-preserving serialized address supports
   bounded index discovery without redefining identity.
6. **Typed physicalities.** One entity carries distinct point, trajectory, factor,
   projection, board, model, and modality physicalities.
7. **Lossless constituent trajectories.** Ordered composition round-trips exact content
   independently of a conventional tokenizer context window.
8. **Mantissa payload geometry.** Typed ids, ordinals, run lengths, flags, and scores
   travel in deterministic GeometryZM-compatible vertices.
9. **Realized semantic curves.** Fréchet/Hausdorff/shape operations run on ordered
   constituent coordinates rather than packed manifests.
10. **Exact occurrence by indexed containment.** One canonical entity point reaches
    every containing trajectory, enabling corpus-wide occurrence queries without text
    scans or ANN guesses.
11. **Perfcache as derived ROM.** Deterministic mmap structures accelerate canonical
    data while PostgreSQL remains the system of record.

## Evidence, relations, and consensus

12. **Universal attestation reduction.** Facts from corpora, users, tools, games, code,
    and checkpoints share subject/relation/object/source/context/outcome evidence.
13. **Content/evidence/consensus separation.** Identity converges, witnesses remain
    isolated, and typed propositions acquire one standing fold.
14. **Glicko-2 epistemology.** Rating, deviation, volatility, source trust, witness count,
    and conservative score represent support and uncertainty.
15. **Signed three-valued testimony.** Confirmation, draw/unknown evidence, and
    refutation remain distinct instead of collapsing absence into falsehood.
16. **Provenance/aggregation duality.** Context-scoped occurrences and globally folded
    propositions coexist without erasing one another.
17. **Record-versus-calculate boundary.** Literal observation and versioned analysis
    compete transparently as different witnesses.
18. **Tier-correct attestation.** Each fact attaches to the compositional unit the source
    actually asserts.
19. **Governed relation registry.** Typed relations, aliases, append-only highway bits,
    salience bands, rank, and physical hotness remain explicit axes.
20. **Trust-class lattice.** Curated corpora, models, tools, responses, and feedback have
    declared source semantics rather than an untyped global weight.

## Ingest and the knowledge mesh

21. **One ordered ingest spine.** Unpack, stream, deduplicate, accumulate, descend,
    apply/COPY, and fold complete as one deterministic protocol.
22. **Pure omni-modal decomposers.** Every source emits the same `SubstrateChange`
    algebra and owns no SQL write implementation.
23. **Interlingual convergence mesh.** Lemmas, senses, concepts, frames, roles, and
    multilingual surfaces link through canonical ids and typed witnesses.
24. **Narrow parser authority.** Tree-sitter and format parsers unpack structure; they do
    not become semantic truth.
25. **Seed profiles as capability contracts.** Foundation, linguistic, conversational,
    code, model, and domain seeds declare what the resulting substrate can prove.

## Chess and games as executable trajectories

26. **Outcome bit identity.** Game outcomes and epistemic outcomes share the same signed
    fold domain.
27. **Move/position consensus.** Transpositions converge while per-game/player/source
    occurrences remain attributable.
28. **Board modality ladder.** Pieces, squares, moves, positions, lines, and games form a
    deterministic content/trajectory hierarchy above Unicode.
29. **Flat-cost witnessed depth.** Completed trajectories deposit deep outcomes so later
    reads can use folded evidence rather than replaying every search online.
30. **Game as forward-pass program.** Compose state, orient task, propose legal actions,
    steer with evidence, select, realize, and witness.
31. **Annotations as shared entities.** Commentary concepts connect dictionary meaning,
    move instances, sequence context, and evaluation testimony.

## Conventional models as substrate witnesses

32. **Checkpoint-as-source.** SafeTensors/config/tokenizer/tensor structure enters as
    source-scoped recorded testimony rather than an opaque runtime authority.
33. **Circuit testimony trajectories.** Layers, heads, experts, factors, projections,
    token effects, and completions are queryable with exact provenance.
34. **Structural-versus-functional circuit identity.** Ordinal coordinates enable
    comparison; functional correlation is separately calculated from evidence.
35. **Cross-architecture circuit cube.** Entity × source × plane × circuit queries align
    incompatible causal, encoder, reranker, MoE, and multimodal models.
36. **Model round table as pooled consensus.** Heterogeneous sources contribute before
    one answer path; no tensor merge, equal architecture, vote, or judge is required.

## Substrate-native model construction

37. **The witnessed web as the Laplacian.** The typed, uncertainty-weighted evidence
    graph supplies the spectral semantic basis.
38. **Architecture derived from evidence.** Relation bands, tiers, hops, modalities,
    factors, trajectories, and recipes determine layer/head/operator schedules.
39. **Glicko-complete edge weights.** Support, uncertainty, refutation, source trust, and
    witness saturation all reach construction and inference.
40. **Source-scoped synthesis.** A, B, or pooled A+B constructions are reproducible
    diagnostic/export scopes without retraining.
41. **Conditional continuation floor.** Exact attested continuation distributions and
    typed backoff ground output selection.
42. **Typed residual/frontier.** Surface, lexical, concept, frame, circuit, code, and
    domain strata remain named rather than anonymously superposed.
43. **Geometry as operator evidence.** Angular, trajectory, containment, Hilbert,
    Procrustes, and related physical operations contribute under typed contracts.
44. **Recipe-driven decomposable export.** Every constructed slot traces to substrate
    cells, calculations, scope, and deterministic recipe inputs.
45. **Codec-independent native model.** SafeTensors and GGUF are external materialization
    formats; they do not define the substrate program.

## Inference, conversation, code, and serving

46. **The walk as forward pass.** Indexed graph/geometry/trajectory execution carries
    typed evidence, uncertainty, provenance, and active residual state.
47. **A*/hop/fan-out execution axes.** Path cost, depth, branch budget, ordinal
    continuity, relation band, and source scope are explicit rather than hidden attention.
48. **No fixed context-window ontology.** Prompts and histories are content-addressed
    trajectories retrieved by task/evidence scope.
49. **Conversation as a witnessed game trajectory.** Sessions contain ordered turns;
    corrections and dependencies add evidence without deleting history.
50. **Dynamic-frontier generation.** Every emitted constituent changes the next proposal
    and selection state.
51. **Code as player.** Generate, stage, compile/test, witness outcomes, and fold them
    into a subsequent decision through the same operation program.
52. **Explainability as typed receipt.** Answers expose bounded cells, relations, scores,
    source scope, operation stages, selection, and writes.
53. **Closed self-improvement loop.** Prompt, response, tool, evaluation, and feedback
    outcomes deposit through the governed lane and immediately affect standing evidence.
54. **One typed operation ISA.** SQL, MCP, OpenAI, games, code, model inspection, and
    export compose the same operation algebra.
55. **OpenAI-compatible substrate serving.** Compatibility binds roles/parameters/tools
    to Laplace semantics without pretending ignored fields or template recall are model
    inference.
56. **MCP citizenship.** Agents read, witness, give feedback, ingest under governance,
    inspect traces, and invoke the same canonical product program.

## Cross-domain preservation laws

57. **Exact reusable subtrajectory identity.** Repeated ordered subpaths are canonical
    reusable content under their trajectory recipe, not merely matching endpoints. A
    chess segment, AST subtree, media/time-series subpath, circuit factor walk, or
    cognition subprogram may be folded into larger trajectories and expanded losslessly
    while each containing occurrence/path remains independently attributable.
58. **Identity-versus-realization separation.** SAN/PGN/FEN, human labels, aliases,
    languages, codec fields, source paths, and model tensor names are contextual
    realizations/references. They never own canonical content identity; typed structure
    plus context may realize a human representation at the read edge.
59. **Composition/trajectory/occurrence symmetry.** Text, chess, models, code, and other
    modalities instantiate the same recursive pattern: atom → composition → higher
    composition → trajectory/reusable structure → witnessed occurrence. Domain grammar
    changes the typed ladder, not the underlying machine law.
60. **Consumer-role model anatomy.** Q/K/V/O, embeddings, FFN Gate/Up/Down, MoE/MLA
    roles, norms, heads, experts and similar checkpoint structures are witnessed
    conventional-architecture roles and optional target roles; they are not the native
    ontology of Laplace cognition.
61. **Deterministic execution trajectory.** A conventional model forward pass, chess
    calculation, tablebase probe, code execution, or other deterministic provider run is
    an exact/declared transformation trajectory under its complete input, generation,
    numeric/precision, implementation and recipe boundary. A result-changing omitted
    coordinate belongs in that boundary.
62. **Information-shaped materialization.** Store irreducible admitted information once,
    reuse canonical compositions, derive deterministic views, and materialize only
    measured rebuildable accelerations or required consumer artifacts. Occurrence volume
    and pair-query combinatorics do not justify duplicate canonical content or eager V² /
    world-all-pairs persistence.
63. **Navigable entity world.** Canonical content, occurrences, provenance, calculations,
    testimony and realizations are addressable so product actions can navigate among
    them through common semantic operations. Opening a historical chess playing at a
    matched exact segment is one instance of the same law used to recenter a document,
    person, model circuit, code artifact or prior receipt; UI state does not own the
    semantics.
64. **Whole-prompt-root cognition and source/participant separation.** A prompt is first
    one exact canonical observation/trunk. UAX/grammar structure supports descent and
    rise but does not privilege a noun, topic, token, punctuation class or English regex
    as the interpretation root. All `UserPrompt` observations share one seeded
    source-type prior while individual users/witnesses retain separately earned typed
    standing, history, preferences and habits. Constituents do not inherit source trust.
65. **Structural geometry/semantic-web orthogonality.** S³ coordinates, trajectory
    geometry, Hilbert locality, angular/curve calculations and related indexes describe
    or nominate structural state. Typed semantic relations, testimony, deterministic
    calculations, dependence and standing remain distinct; geometry cannot become
    meaning merely because it is measurable or indexable.
66. **Falsifiable operator-generation law.** Sparse intersections/dot products,
    incidence/transport, Laplacian/spectral methods, Lanczos, QR/Gram-Schmidt, SVD,
    Procrustes, geometric metrics, Glicko-2, A*/best-first and other named mathematics
    apply only through explicit typed contracts naming their input state, assumptions,
    scope/evidence roots, direction/roles, algebra/numeric boundary, resources, output,
    loss/approximation, provenance/receipt and deliberate counterexamples. Target-neutral
    operators are derived first; consumer Q/K/V/O/FFN/etc. materialization comes second.

## Product identity

Laplace replaces opaque probabilistic runtime authority with content-addressed identity,
lossless physicality, source-retaining testimony, uncertainty-bearing consensus, typed
CPU-native execution, and deterministic receipts. Models, corpora, conversations, code,
games, and tools are participants in one witnessed world rather than disconnected
pipelines.
