-- Labelled gloss, the default answer when the caller named no read shape.
DROP FUNCTION IF EXISTS recall_fallback_gloss(text, bytea);

CREATE OR REPLACE FUNCTION recall_fallback_gloss(p_topic bytea, p_context_ids bytea[] DEFAULT NULL)
    RETURNS TABLE(reply text, eff_mu numeric, witnesses bigint)
    LANGUAGE sql STABLE PARALLEL SAFE
    AS $$
    SELECT @extschema@.label(p_topic) || ': ' || d.definition, d.eff_mu, d.witnesses
    FROM @extschema@.define_fast(p_topic, COALESCE(p_context_ids, ARRAY[]::bytea[]), 3) d
$$;
