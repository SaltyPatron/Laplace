diff --git a/DESIGN.md b/DESIGN.md
index 5e69058..17ff3e5 100644
--- a/DESIGN.md
+++ b/DESIGN.md
@@ -245,7 +245,7 @@ The substrate is implemented across three layers, each owning one kind of work (
 | | **`Laplace.Decomposers.Abstractions`** | `IDecomposer` C# plugin interface per [ADR 0051](docs/adr/0051-idecomposer-csharp-plugin-contract.md) — every per-source decomposer (`Laplace.Decomposers.Unicode/.WordNet/.OMW/.UD/.Wiktionary/.Tatoeba/.ConceptNet/.Atomic2020/.TreeSitter/.Model`) implements it. Streaming-first async; layer-ordering aware per [ADR 0037](docs/adr/0037-layered-seed-ingestion-and-model-codec-fidelity.md); bootstrap responsibility per [ADR 0042](docs/adr/0042-bootstrap-order-and-substrate-canonical-seeding.md); cross-decomposer convergence automatic via content-addressing + ON CONFLICT. | `app/Laplace.Decomposers.Abstractions/` (planned) |
 | | **`Laplace.Decomposers.Containers.Abstractions`** | `IContainerParser` interface per [ADR 0055](docs/adr/0055-static-structural-parse-exploded-view.md) — per-format static parsers (Safetensors / PyTorchPickle / Onnx / TfSavedModel / YOLO native / HDF5 / ZIP / TAR / Jupyter / etc.). Substrate **never loads** container files; always statically dissects to an exploded view of substrate entities + typed structural attestations. No code execution, no framework-loader invocation, no security policy needed (no attack surface). | `app/Laplace.Decomposers.Containers.Abstractions/` (planned) |
 | | **`Laplace.Ingestion`** | `IngestRunner` orchestration loop per [ADR 0052](docs/adr/0052-ingest-pipeline-orchestration.md). Composes `IDecomposer` (per-source) + `SubstrateCRUD` (write surface) into the canonical per-source ingest recipe: layer-ordering enforcement + bootstrap + streaming intent application + checkpoint/resume + transient retry + cancellation + parallel-worker variant + per-intent Prometheus metrics. | `app/Laplace.Ingestion/` (planned) |
-| | `Laplace.Cli` | CLI subcommands: `cascade`, `synthesize`, `ingest`, etc. `just ingest <source>` resolves source → IDecomposer → IngestRunner.RunAsync. (lands Chunks 5/7). | `app/Laplace.Cli/` (planned) |
+| | `Laplace.Cli` | CLI subcommands: `cascade`, `synthesize`, `ingest`, etc. `just ingest <source>` resolves source → IDecomposer → IngestRunner.RunAsync. | `app/Laplace.Cli/` (planned) |
 | | `Laplace.Endpoints.*` | Protocol-endpoint plugins (OpenAI-compat, etc., per [ADR 0011](docs/adr/0011-polymorphic-plugin-architecture.md)). | `app/Laplace.Endpoints.*/` (planned) |
 | | `Laplace.Sources.*` | `ISource` plugins per modality / per-corpus. | `app/Laplace.Sources.*/` (planned) |
 | | `Laplace.Decomposers.<Source>` | One `IDecomposer` implementation per source layer per [ADR 0037](docs/adr/0037-layered-seed-ingestion-and-model-codec-fidelity.md): `Unicode` (Layer 1), `ISO` (Layer 2), `WordNet` (Layer 3), `OMW` (Layer 4), `UD` (Layer 5), `Wiktionary` (Layer 6), `Tatoeba` (Layer 7), `Atomic2020` + `ConceptNet` (Layer 8), `TreeSitter` (Layer 9), `Model` (composite per [ADR 0043](docs/adr/0043-composite-decomposer-architecture.md), Layer 10). Each project owns: its source's pre-canonicalization protocol; its source's type vocabulary + attestation-kind vocabulary bootstrap; calls into `TextDecomposer` + `HashComposer` (engine primitives) + `SubstrateCRUD` (via `IngestRunner`). | `app/Laplace.Decomposers.<Source>/` (planned) |
@@ -505,7 +505,7 @@ int sparsity_probe_validate(const double* weights, size_t n,
 
 ### `liblaplace_synthesis` — recipe + architecture templates + native packages (engine/synthesis/)
 
-All opaque-handle plugin interfaces per ADR 0011 + R10. Real implementations land Chunks 7-8.
+All opaque-handle plugin interfaces per ADR 0011 + R10. Real implementations land as work tracks the v0.1 milestone per [ADR 0060](docs/adr/0060-retire-chunk-sequence-v0.1-milestone-cadence.md).
 
 ```c
 // recipe.h — parsed Recipe entity from config.json + overrides (ADR 0009)
@@ -634,7 +634,13 @@ The C/C++ engine turns that tuple stream into an arena-aware effective score and
 
 ## VI. Polymorphic plugin interfaces
 
-Implementations live in `engine/src/`. See [RULES.md R10](RULES.md).
+Six plugin interfaces per [RULES.md R10](RULES.md). Placement is **per-interface**, not uniform:
+
+- `IDecomposer` is a **C# plugin contract** per [ADR 0051](docs/adr/0051-idecomposer-csharp-plugin-contract.md). The C++ sketch below is **historical / superseded**; the live contract lives at `app/Laplace.Decomposers.Abstractions/IDecomposer.cs`. The engine-side text-decomposition primitive split out to `TextDecomposer` per [ADR 0047](docs/adr/0047-text-decomposer-pure-primitive.md); the leaf-to-trunk content-addressing primitive split out to `HashComposer` per [ADR 0048](docs/adr/0048-hash-composer-leaf-to-trunk.md).
+- `IFormatWriter` is a **C# plugin contract** per [ADR 0059](docs/adr/0059-format-writer-emission-matrix-and-ifw-contract.md). The C++ sketch below is **historical / superseded**; the live contract lives at `app/Laplace.Synthesis.FormatWriters.Abstractions/IFormatWriter.cs`. Engine-side hot-path serialization (safetensors tensor I/O; GGUF block-quantization; AWQ per-channel scale computation) remains C/C++ in `liblaplace_synthesis` per [ADR 0024](docs/adr/0024-engine-modularization.md) + [ADR 0027](docs/adr/0027-separation-of-concerns-invariants.md); the C# writer thin-wraps via P/Invoke.
+- `ISource`, `IArchitectureTemplate`, `IFeatureExtractor`, `IProtocolEndpoint` remain as the C++ engine-side sketches below, with corresponding C# bindings in `Laplace.Engine.{Core,Dynamics,Synthesis}` per [ADR 0026](docs/adr/0026-csharp-project-structure.md). No supersession on these four.
+
+Historical C++ sketches (kept for the four interfaces still C++-side; `IDecomposer` + `IFormatWriter` superseded as noted above):
 
 ```cpp
 class ISource {
diff --git a/README.md b/README.md
index 40196b0..b50dd8b 100644
--- a/README.md
+++ b/README.md
@@ -147,10 +147,10 @@ See [STANDARDS.md](STANDARDS.md) for the locked dependency-source table.
 
 | | |
 |---|---|
-| **Current chunk** | Chunk 0 ✅ done → **Chunk 1 in queue** ([#1](https://github.com/SaltyPatron/Laplace/issues/1)) |
+| **Current focus** | [v0.1 milestone](https://github.com/SaltyPatron/Laplace/milestone/1) — TinyLlama round-trip (per [ADR 0060](docs/adr/0060-retire-chunk-sequence-v0.1-milestone-cadence.md), chunks retired as a sequencing mechanism) |
 | **Milestone** | [v0.1.0 — Chattable Qwen3 Roundtrip](https://github.com/SaltyPatron/Laplace/milestone/1) |
 | **Open issues** | [github.com/SaltyPatron/Laplace/issues](https://github.com/SaltyPatron/Laplace/issues) |
-| **All chunks** | [Issues filter: chunk-*](https://github.com/SaltyPatron/Laplace/issues?q=is%3Aopen+label%3A%22type%3Aenhancement%22) |
+| **Open epics** | [Issues filter: epic](https://github.com/SaltyPatron/Laplace/issues?q=is%3Aopen+label%3Aepic) |
 | **Recent CI runs** | [github.com/SaltyPatron/Laplace/actions](https://github.com/SaltyPatron/Laplace/actions) |
 
 ## Getting started
