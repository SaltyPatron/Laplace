# Archived specification set

These are the complete annotated files that previously lived in `docs/specs/`. They
mix normative clauses with dated measurements, corrections, issue status, and execution
logs, so they are preserved for history but no longer authoritative.

Clean current contracts with compatibility filenames live in `docs/specs/`.
