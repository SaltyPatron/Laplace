DROP FUNCTION IF EXISTS laplace.render_text_fast(bytea, integer);
CREATE OR REPLACE FUNCTION realize.render_text_fast(p_id bytea, p_max_depth integer DEFAULT 8)
    RETURNS text
    AS 'MODULE_PATHNAME', 'pg_laplace_render_text_fast'
    LANGUAGE C STABLE;
