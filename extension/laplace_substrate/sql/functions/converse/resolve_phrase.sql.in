DROP FUNCTION IF EXISTS laplace.resolve_phrase(text);
CREATE OR REPLACE FUNCTION converse.resolve_phrase(p_phrase text)
    RETURNS bytea
    AS 'EXECUTION_LIBRARY', 'pg_laplace_resolve_phrase'
    LANGUAGE C STABLE STRICT;
