name: Data — documents ingest
run-name: "Documents ingest — ${{ inputs.corpus }} → ${{ inputs.database }}"

on:
  workflow_dispatch:
    inputs:
      corpus:
        description: "Document collection"
        type: choice
        required: true
        default: project-gutenberg
        options: [project-gutenberg, fixtures, electronics, smoke-alice, custom-path]
      advanced_path_override:
        description: "ADVANCED: local file/directory override; required only for custom-path"
        type: string
        required: false
        default: ""
      database:
        description: "target database"
        type: choice
        options: [laplace]
        default: laplace
      run_idempotency:
        description: "re-ingest and prove no double-count"
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  resolve:
    runs-on: [self-hosted, laplace]
    timeout-minutes: 2
    outputs:
      path: ${{ steps.pick.outputs.path }}
    steps:
      - id: pick
        env:
          CORPUS_INPUT: ${{ inputs.corpus }}
          PATH_INPUT: ${{ inputs.advanced_path_override }}
        run: |
          set -euo pipefail
          root="${LAPLACE_DATA_ROOT:-/vault/Data}"
          if [[ -n "$PATH_INPUT" ]]; then
            [[ -e "$PATH_INPUT" ]] || { echo "::error::$PATH_INPUT does not exist"; exit 1; }
            echo "path=$PATH_INPUT" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          case "$CORPUS_INPUT" in
            project-gutenberg) p="$root/ProjectGutenberg/text" ;;
            fixtures) p="$root/test-data/text" ;;
            electronics) p="$root/test-data/electronics" ;;
            smoke-alice) p="$root/ProjectGutenberg/text/alice.txt" ;;
            custom-path) echo "::error::custom-path requires the advanced path override"; exit 2 ;;
            *) echo "::error::unknown corpus $CORPUS_INPUT"; exit 1 ;;
          esac
          [[ -e "$p" ]] || { echo "::error::$p does not exist"; exit 1; }
          echo "path=$p" >> "$GITHUB_OUTPUT"

  seed:
    needs: resolve
    uses: ./.github/workflows/seed.yml
    with:
      mode: source
      source: document
      path: ${{ needs.resolve.outputs.path }}
      database: ${{ inputs.database }}
      run_gate: true
      run_idempotency: ${{ inputs.run_idempotency }}
