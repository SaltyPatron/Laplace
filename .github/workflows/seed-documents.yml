name: Seed — documents

on:
  workflow_dispatch:
    inputs:
      corpus:
        description: "document collection"
        type: choice
        required: true
        default: project-gutenberg
        options: [project-gutenberg, fixtures, electronics, smoke-alice, path-override]
      path:
        description: "explicit path; overrides corpus when present"
        type: string
        default: ""
      database:
        description: "target database"
        type: choice
        options: [laplace]
        default: laplace

permissions:
  contents: read

jobs:
  resolve:
    runs-on: [self-hosted, laplace]
    timeout-minutes: 2
    outputs:
      path: ${{ steps.pick.outputs.path }}
    steps:
      - id: pick
        run: |
          set -euo pipefail
          root="${LAPLACE_DATA_ROOT:-/vault/Data}"
          p='${{ inputs.path }}'
          if [[ -z "$p" ]]; then
            case '${{ inputs.corpus }}' in
              project-gutenberg) p="$root/ProjectGutenberg/text" ;;
              fixtures) p="$root/test-data/text" ;;
              electronics) p="$root/test-data/electronics" ;;
              smoke-alice) p="$root/ProjectGutenberg/text/alice.txt" ;;
              path-override) echo "::error::path-override requires path"; exit 1 ;;
              *) echo "::error::unknown corpus"; exit 1 ;;
            esac
          fi
          [[ -e "$p" ]] || { echo "::error::$p does not exist"; exit 1; }
          echo "path=$p" >> "$GITHUB_OUTPUT"

  seed:
    needs: resolve
    uses: ./.github/workflows/_ingest.yml
    with:
      source: document
      path: ${{ needs.resolve.outputs.path }}
      database: ${{ inputs.database }}
      timeout_minutes: 480
