# Laplace invention deviation ledger — 2026-09-19

## Purpose

This is an evidence-bound map of where the repository has diverged from the invention and which historical defects have already been repaired. It is not a replacement backlog, a global project-status label, or a license to restate old failures forever.

The governing invention remains `docs/INVENTION.md`. Current implementation details are checked against `main` at the revision that contains this ledger.

## Invention mechanics that must remain visible

Laplace is one central knowledge system, not a lookup engine. Canonical identities participate simultaneously in recursive composition, physicality/trajectory, occurrence/containment, typed relations, testimony/consensus, geometry/locality, source/context and deterministic calculation.

Coordinates are not identities or parking spaces. Multiple distinct structures may occupy the same point/region. Ordered identity and trajectory preserve differences; typed metrics such as angular distance, Fréchet/Hausdorff over realized curves, Karcher/centroid placement, relation/evidence channels and occurrence state expose different aspects of the same substrate.

Physicality trajectories are structural information: constituents, order, multiplicity and runs permit containment, co-occurrence, predecessor/follower, gaps, repeated paths, examples and usage to be calculated without blindly materializing every structural fact as an ordinary semantic edge.

The forward computation is the response of overlapping webs to an admitted observation: what responds, by which indexed route, and with what typed force. One walk, continuation table, KNN search, relation query or graph traversal is only an operator inside that machine.

## Current divergences on main

### D1 — parent-coordinate law is split

Native generic composition uses Euclidean centroid placement inside/on the 4-ball. Managed `NgramTrajectory` and current chess composition use Karcher mean on S3. Those recipes can assign different coordinates and therefore different Hilbert addresses to equivalent logical compositions.

Evidence: `engine/core/src/hash_composer.c`, `app/Laplace.Substrate/Abstractions/NgramTrajectory.cs`, `app/Laplace.Chess/Service/ChessCompose.cs`, `docs/ARCHITECTURE.md` section 3.4.

### D2 — canonical forward coupling does not expose every intended geometric/locality plane

The current native forward path has typed physicality integration and extends query state with structural frontier identities under `LAPLACE_QUERY_OPERAND_PHYSICALITY`. The current source audit in #1401 still finds no Hilbert or Fréchet provider hook in the canonical COUPLE/ROUTE path. The math exists elsewhere, but those planes are not participating in the main query-relative response field.

### D3 — semantic-source decomposers have destroyed native schema information

Issue #1153 records measured loss across WordNet, FrameNet, VerbNet, PropBank, Predicate Matrix, OMW, CILI, MapNet/WFN/XWFN and Wiktionary. Information discarded at admission cannot later tug the forward program.

### D4 — document/source-native structure remains a preservation obligation

UAX29 segmentation is not a full document grammar. Paragraph/page/section/chapter/table/figure/AST/dependency/source-native structures must remain available when the source provides them rather than being flattened into one generic text ladder. Evidence: #1010.

### D5 — registry/reference/content state classes remain a cohesion risk

Historical admission put governed/reference identities into ordinary tier positions without a coherent universal physicality/state-class meaning. Current law distinguishes content, governed references, concepts, aliases and realizations, but the live estate and readers still require typed closure/proof. Evidence: #1038 and #1562.

### D6 — generic semantic-only candidate participation still needs exact forward acceptance

The historical generation path could make a semantically strong candidate impossible to select when it was absent from local sequence successors. Current native code has a common query state, routed semantic evidence and state extension after selection. The remaining exact acceptance is narrower: prove that a candidate introduced through generic non-sequence substrate response can enter the coupled frontier and be selected under the canonical program with route/provenance retained. Evidence: `trajectory_generate.c` and #1478.

## Historical defects with current repairs present

### H1 — packed trajectory values were used as geometric paths

An earlier metric implementation fed mantissa-packed identity carriers to Fréchet/Hausdorff. Current `structural.entity_curve()` resolves real child coordinates, and `generation.metric_edges()` measures those realized curves.

### H2 — physicality provenance was turned into repeated testimony/consensus

Current ordinary admission records source/unit/time in `laplace.physicality_observations`; DB acceptance tests require zero generated `HAS_PHYSICALITY` attestations and zero consensus for that ordinary structural provenance path.

### H3 — ingest could mark applied units before all consensus folds completed

The retained `ops.refold_source` incident records this exact failure. Current `ConsensusAccumulatingWriter` defines evidence plus consensus as one apply flow and drains bulk folds before source/run completion.

### H4 — conversation/generation had rival product paths

Historical #921 records divergent speaking paths. Current `generation.forward_program(...)`, `generation.forward_text(...)` and `converse.forward_turn(...)` provide a shared program surface. Remaining acceptance concerns endpoint parity and complete plane participation, not the old claim that no shared forward program exists.

### H5 — legacy Tier-0 placement produced the banding problem

The retired bounded rank/N placement concentrated early ranks into a Hopf-latitude band. Current v4/open placement spreads early prefixes across the shell, stores selected coordinates in the perfcache and rejects legacy banded blobs.

## Status language

Use this ledger to name mechanisms, not to narrate a global negative.

Good: `Hilbert/Fréchet are not wired into canonical COUPLE on this revision.`
Good: `The current ordinary physicality writer records provenance without consensus.`
Good: `WordNet lexical-pointer target-sense scope is still lost by the current decomposer.`

Do not substitute generic refrains such as `Laplace is unfinished`, `Laplace is not delivered`, or `this is not the full invention` for the exact mechanism and next repair.