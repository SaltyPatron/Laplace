namespace Laplace.Engine.Core;

/// <summary>
/// Selects the image/audio ladder for <c>laplace_modality_witness_emit_tree</c>.
/// Media scalar values compose through the shared codepoint/content floor rather
/// than minting arbitrary private atoms. GH #1134 owns the remaining exact
/// occurrence/reconstruction metadata contract.
/// </summary>
public enum MediaLadderKind
{
    Image = 1,
    Audio = 2,
}
